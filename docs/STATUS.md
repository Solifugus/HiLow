# Project Status

> Persistent state record for the HiLow compiler project. Claude Code reads this at the start of every session and updates it at the end. For session-by-session detail, see `SESSIONS.md`. For the session-start brief, see `CLAUDE.md`.

---

## Current state

**Phase: 6c COMPLETE (2026-07-23) — crash robustness. THE CELL MIGRATION IS COMPLETE; NO PHASE IS CURRENT. GOVERNED BY `docs/phase6-brief.md`** (R-A–R-E + 6c's c1–c4), SUPERSEDES audit §6. 6c changed **no compiler and no runtime code** — it asserts that what 6a/6b built already survives violent death, and the fixtures confirmed it on the first run with no fix required (the crash properties were previously believed; they are now pinned). Rulings: **c1** persistence is SEMANTIC (unlink-on-last-detach REJECTED — it would break the 6a persistence fixture); **c2** cleanup is explicit and out-of-language (documented, not implemented); **c3** no new language surface — liveness-as-a-watchable deferred for want of a spec process-identity concept; **c4** the inbox-bypassing pull is sound for scalars only, recorded as a shared-containers revisit precondition. DELIVERED: three crash fixture sets (xproc harness 7 → **10**) — SIGKILL the producer mid-write-loop (a live consumer verifies no delivered value goes backwards, exiting on a fire-count FLOOR not an exact count; then a fresh survivor proves the abandoned segment verifies, holds its last write, and does not wedge the pull), SIGKILL an attached consumer 400ms into the producer's 500000-write burst (~1.5s measured, so the kill provably lands mid-burst; producer completes and a fresh reader sees exactly 500000 — R-B made executable), and full population turnover (violent death + normal exit, ZERO attachers for an interval, value 42 survives — ruling c1 made executable). Harness gained `native_child` (victims aren't valgrinded — a SIGKILLed process never reaches `ERROR SUMMARY`; every SURVIVOR still is) and `kill_and_reap`, which ASSERTS the victim was still running, so a victim that exited early fails the test rather than passing vacuously. SPEC: watcher-body idempotency guidance (at-least-once means a body may run more times than there were changes; the 6b threshold double-fire is the worked example, both fixes shown) + a Segment Lifetime and Cleanup subsection (`/hilow.<name>`, flat per-user namespace, 0600, `ls`/`rm` on `/dev/shm`, unlinking a live segment silently splits its users, no reboot survival). Existing corpus untouched (no compiler change). REJECTION_FIXTURES 32; ignored 1; KNOWN_MEMORY_BUGS empty. Phase 6 closure (guarantees vs non-guarantees): state-of-migration §K.3. Adjudications: §K.2. **NEXT: NOTHING IS SCHEDULED — owner direction required.** Phase 6 is the LAST phase in the audit, so "advance to the next audit phase" has no referent; which development-plan phase resumes is an owner decision (indicated candidate, not a ruling: **9b Time Type**, the destination of the sole remaining `#[ignore]`). **OPEN FOR ADJUDICATION: the spec's `for`-loop divergence** — the spec documents C-style `for` (10 occurrences) and single-variable `for (let x in y)` (8) as features, but the parser implements ONLY the tuple form `for (let (i,v) in expr)` (5); 18 of 23 spec `for` loops do not compile. The mini-brief's prescribed sweep ("convert C-style to iterator form") is VOID because its target form is itself unimplemented, so the sweep was NOT performed — see Open questions. Superseded header below (6b):

**Phase: 6b COMPLETE (2026-07-23) — cross-process DELIVERY. GOVERNED BY `docs/phase6-brief.md`** (rulings R-A–R-E), SUPERSEDES audit §6. Phase 6a (placement) + 6b (delivery) both landed. The watch side: the declaring thread keeps one record per watched placed cell `{cell, last_seen_epoch, last_delivered}` and, at the EXISTING drain safe points (runtime entries + loop back-edges — no new safe points), pulls the segment epoch; on advance it delivers locally via the 5c notify path. The pull UNIFIES cross-thread-local and cross-process delivery (a placed cell's cross-thread notify skips the inbox — the declaring thread's pull delivers; multi-thread-watching correct for free). DEDUP protects R1 exactness: a same-thread write fires synchronously and advances the record so the pull skips it. Fire semantics: `(assigned)` on epoch advance (at-least-once, coalescible — fire count ≠ remote-write count, never asserted); `(changed)` iff drain-time value ≠ last-delivered (ABA within a window is silent). Idle backoff (yield→bounded sleep ≤1ms); residual delivery at final drain. The spec's Cross-Process Watchers example is a real 2-program fixture that prints "Done!" (spec text edited: named `shared("counter")` + its C-style `for` — which HiLow never implemented — changed to `while`; see §I.3/J.2 6b-v, FLAGGED). Existing corpus BYTE-IDENTICAL (32 at-risk async/shared/loop programs, 0 diffs vs 6c530a8). Test infra: xproc_harness grew to 7 (share×2, dedup same/cross, prodcons, watcher-exits, spec-example — all under valgrind, both processes); shm_unit 2 (now 7 C cases incl. the pull's changed/assigned + ABA pinned deterministically). REJECTION_FIXTURES 32; ignored 1; KNOWN_MEMORY_BUGS empty. Adjudications: state-of-migration §J.2. NEXT: **6c — lifecycle** (mini-brief to be written after 6b per R-E: segment cleanup policy, process-liveness-as-watchable/pid-as-cell; docs/phase6-brief.md §3-6c is a sketch only). Superseded header below (6a):

**Phase: 6a COMPLETE (2026-07-23) — cross-process `shared` by placement. GOVERNED BY `docs/phase6-brief.md`** (rulings R-A–R-E), which SUPERSEDES audit §Phase 6. Phase 5 CLOSED. Cross-process `shared` is a typed slot in a named POSIX shm segment mapped by every participant — **placement, not transport** (nothing serialized/sent). Surface `shared("name") let x: i32 = 0` (bare `shared` unchanged = 5c cross-thread); the "sendable check" landed as **placeability** rejection diagnostics, not a phase. DELIVERED: parser/AST (`LetDecl.shared_segment`), placeability fence (container→pointer-bearing, watcher→watcher-bearing) + name validation ([A-Za-z0-9._-]+, 1–64), runtime versioned-header shm segment (`HiLowCell.shm`; create-or-attach per R-D — creator inits + release-publishes init-complete, attacher bounded-waits + verifies every header field + observes/fires-nothing; persistent, 0600; accessors gain a mapped-slot branch; write bumps epoch though nothing pulls until 6b; placed ⊃ shared), `hl_scalar_new_i32_placed` codegen + `-lrt` linkage, a two-process valgrind harness (both orderings prove creator-inits/attacher-ignores-init/persistence/sharing), and a C-fork unit harness for the paths HiLow can't reach (type/magic mismatch, timeout, empty-name — i32-only + `""` doesn't lex). Existing corpus BYTE-IDENTICAL (322 compiling programs, 0 diffs vs 84f707b — every codegen change gated on placement). REJECTION_FIXTURES 28 → **32**; ignored stays 1; KNOWN_MEMORY_BUGS empty. Adjudications: state-of-migration §I.2. NEXT: **6b — delivery** (epoch-pull at existing drain safe points, R3 semantics; docs/phase6-brief.md §3-6b). Not in 6a/6b: shared containers, multi-var segments, 6c lifecycle. PARKED questions from the aborted standalone-6a recon (shared-in-6a status, boundary-locality) are resolved by the brief: the check is a per-declaration placeability fence (R-A), not a transitive call-site analysis — there is no boundary to launder through because there is no send. NOT in scope: 6b delivery, 6c lifecycle, shared containers, multi-var segments (brief §6). Last landed code: the 2026-07-23 maintenance commit (docs + test infra only — ledger repair, valgrind gate lane-split, doc-truth sweep); this ratification commit is docs-only (adds phase6-brief.md, wires pointers).

Phase: Cell migration Phase 5c COMPLETE — `shared` scalars; **this closes Phase 5** (notification queues + concurrency). A `shared let` i32 scalar is refcounted + atomic + cross-context watchable. `HiLowCell` gained `sub_lock` (`pthread_mutex_t*`, non-NULL ⟺ shared): `hl_scalar_new_i32_shared` allocates it; the payload is accessed atomically (`hl_scalar_get_i32` atomic-loads, `hl_cell_set_i32` atomic-exchanges — both branch on `sub_lock`; a shared set skips the racy `cell_has_audience` pre-check and always routes through `hl_cell_notify`; plain `x += 1` is two atomic ops = a racy RMW, the prover's future warning, `(atomic-add)=` deferred); and the subscriber list is guarded — `hl_cell_subscribe`/`hl_cell_unsubscribe_watcher[_origin]` take the lock, `hl_cell_notify` snapshots the list under it then fires the snapshot UNLOCKED (collect-then-fire made cross-thread-safe; the lock is never held while firing/enqueuing). Non-shared cells keep `sub_lock == NULL` → no lock, no atomics, notify/subscribe fast path byte-for-byte unchanged. Threaded mode is engaged by `async` OR `shared` (`threaded_mode()`; verified a shared-without-async program trips it — fixture `shared_single_thread`). DEVIATION 5a-ii GRADUATED (its own change, C-harness-proven first): `HiLowCell` gained a `kind` tag (SCALAR/ARRAY/OBJECT, set at the 4 constructors); each typed release's teardown was factored into a `*_finalize`; new `hl_cell_release_full` dispatches on `kind` at refcount 0; the inbox's cell release routes through it — so a cross-thread last-release of a shared cell frees the typed container from any thread (harness cases `case_inbox_sole_owner_frees_{array,scalar}` each leak 152B with pre-fix `hl_cell_release`, clean with the fix). SPEC R4 (`docs/hilow-design.md`): `shared` unified to one meaning (refcount + atomic + cross-context watchable), reconciling the memory-mode text with the concurrency text. SCOPE FENCE (rejections, pinned): shared containers (codegen diagnostic, `shared_container_rejected`) and `(deep)` across `shared` (subsumed by the typecheck deep-requires-container rule since shared is scalar-only, `shared_deep_rejected`) — REJECTION_FIXTURES 26 → 28. Codegen: `uses_shared` pre-scan → `threaded_mode()`; `shared_vars` populated in the program-body pre-pass; the boxed-i32 let uses `hl_scalar_new_i32_shared` for shared vars (reads/writes need NO codegen change — the runtime branches on `sub_lock`); `-DHILOW_THREADED` keyed on `threaded_mode()`. Boxing: `shared` lets box (`BoxReason::Shared`) — a shared scalar is a cell whether or not watched. Five order-insensitive shared fixtures (reactive counter under contention / watch-declared-mid-production / ended-watcher-on-locked-cell / exit-release / shared-without-async), each blocking main on a main-thread flag for deterministic output, all valgrind-clean; single-threaded corpus byte-identical vs bc51de4. Next: Phase 6a — process tier / sendable check (compile-time: no raw pointers/watchers/open handles cross a process boundary; rejection tests first — cell-migration-audit §Phase 6).

Phase: Cell migration Phase 5b COMPLETE — minimal `async`. An `async { ... }` block (High mode; `Statement::Async`, parsed like `stealth`) compiles to a `void* fn(void*)` pthread entry emitted into a new `async_bodies` buffer before `main`, spawned via `hl_async_spawn` and joined at program exit (`hl_async_join_all`, no detached threads). Outer variables the body references are captured into a heap env struct (the boxing analysis gained an `async` capture boundary — `walk_async`, reusing the watcher-boundary machinery, so an async-referenced outer var boxes and is reachable as a static/heap cell); the env is packed with retained refs at the spawn site and released when the body finishes. A boxed program-scope scalar is reachable as its file-scope static, but a program-scope container is a main-local, so the env is the uniform path for both. Mutating a watched cell from the spawned thread stores on that thread then routes through `hl_cell_notify` → the declaring thread's inbox (the phase's whole point); **same-thread firing stays synchronous (R1)** — the inbox is the cross-thread path exclusively (owner AskUserQuestion 2026-07-21; the brief permits either, 5b picks synchronous). THREADED RUNTIME MODE, engaged iff the program contains `async` (detected by a pre-scan `program_body_has_async` before any code emits): (a) refcount inc/dec become atomic RMW via `HL_RC_INC`/`HL_RC_DEC` macros (`-DHILOW_THREADED`, applied to both main.c and runtime.c in one `cc` call); (b) `hl_alloc_count`/`hl_free_count` become `_Atomic int` so the generated leak-check counters (unchanged `hl_alloc_count++` bytes → atomic because the declared type is atomic) don't lose updates to a race; (c) a codegen-emitted loop-backedge safe point (`hl_thread_safepoint()` at the top of `for`/`while`/`loop` bodies — brief §5b amendment) so a pure-compute `loop { }` event loop drains its inbox. ALL threaded-mode emission is gated on `uses_async`, so a program without `async` emits byte-identical C (mode switch, not hope) — VERIFIED: full-corpus C-diff vs 1c3bd2f = 352/352 successfully-compiling single-threaded programs byte-identical; the only diffs are the 5 new async fixtures (new programs) + `modules/diamond` (the pre-existing module-emission-order nondeterminism — diffs against itself on base AND new, 0 async artifacts, one new variant byte-matches base). Program exit (both the explicit-`return` path and the fall-through epilogue): join all async threads (each does its own `hl_thread_final_drain`), then main `hl_thread_final_drain`s last (join is the synchronization edge → producers' enqueues are visible), before scope cleanup so residual fires see live cells. The `#[ignore]`d re-entrancy test ACTIVATED (ignored 2 → 1; renamed `watcher_reentrant_deferred` → `watcher_reentrant_sync`, expected rewritten to the synchronous/nested output the current binary already produces). Five order-insensitive cross-thread fixtures added (threshold drain, MPSC multi-producer, (added) accumulation/R3, ended-watcher-drops/R5, exit-join), each blocking main on a main-thread-updated flag for deterministic output, all valgrind-clean. Mode is parsed but not enforced anywhere in the checker, so async's "High mode" designation is documentation — a Low-mode gate would be new infrastructure with no precedent, deferred. Next: Phase 5c — `shared` scalars (atomic access + cross-context watchable; the `docs/hilow-design.md` keyword-unification edit R4; resolves 5a-ii; shared containers and `(deep)` across `shared` rejected).

Phase: Cell migration Phase 5a COMPLETE — notification-queue infrastructure (no language surface). The four process-global statics migrated to `_Thread_local` (`hl_stealth_depth` stays a named global — codegen emits it — but is now thread-local; `hl_notify_depth`/`hl_deep_epoch`/`hl_deferred_releases` too). A per-thread `HiLowThreadContext` (mutex-guarded global registry) holds an MPSC, mutex-guarded **inbox**: entries keyed by watcher identity, coalescing on re-enqueue with **mandatory delta accumulation** (R3: bare (changed)/(assigned) collapses to a pending flag; (added)/(removed)/(moved) accumulate as a per-entry delta list delivered in order at drain via the existing one-delta body ABI). `HiLowWatcher` gained `owner_ctx`; `hl_cell_notify` routes each fire via `hl_notify_deliver` — same-thread → the existing synchronous fire (byte-for-byte, R1); other-thread → `hl_inbox_enqueue` (retains the watcher per R6 and the cell per axiom 5; takes delta ownership). Drain (`hl_thread_drain_inbox`) detaches under the lock then fires outside it; ended watchers drop WITHOUT firing (R5); it is legal only at `hl_notify_depth==0`. Safe-point drain hooks (`hl_thread_safepoint`) at allocation (`hl_object_new`, `hl_array_new`) and syscall/output (`print_str`) boundaries — DORMANT single-threaded (inbox never nonempty), so no-ops. `cc` gained `-pthread`. PROOF: C-level inbox unit tests (tests/inbox_unit_harness.c driven by tests/inbox_unit.rs) — enqueue/drain, coalesce+accumulate, distinct-watchers-don't-coalesce, ended-drops-without-firing, teardown — pass and are valgrind-clean (definite,indirect). RESOLVED FORK (owner AskUserQuestion): drains live at RUNTIME safe points (allocation/syscall), NOT codegen-emitted statement boundaries — so program-logic codegen is byte-identical (no `codegen/mod.rs` change). DEVIATIONS (recorded in state-of-migration §5a): (1) the four statics are `_Thread_local` file-scope globals rather than struct fields — the semantic invariant (per-thread) holds and `hl_stealth_depth` MUST stay a named global for byte-identical codegen; the `HiLowThreadContext` holds the inbox+registry, which is what actually needs cross-thread discovery. (2) The inbox owns a `+1` on the entry's cell via `hl_cell_retain`/`hl_cell_release`, but `hl_cell_release`-at-0 does only cell-header teardown (the typed container release frees the struct), so the inbox must not be the cell's SOLE owner at drain — UNREACHABLE in single-threaded 5a (inbox dormant) and resolved in 5c when the only cross-thread cells are typed `shared` scalars. (3) The entry snapshots `body_fn`/`env`/`cell`/`event` in addition to the brief's `(watcher, delta, pending)` triple — the fire needs them (the body ABI is `body(env, cell, delta)`); `env` == the retained watcher's env. Single-threaded corpus byte-identical; valgrind gate green; KNOWN_MEMORY_BUGS empty; ignored stays 2 (re-entrancy → 5b; unknown-with-options → 9b). Next: Phase 5b (minimal `async`).

Phase: Cell migration Phase 4a COMPLETE — statement temporaries unified onto one per-statement release mechanism; Phase 4b COMPLETE (2026-07-20, 4-series closeout) — the match-arm borrowed-into-Owned use-after-free found by the Phase-5 preflight is extinguished.
Status (4a): PREMISE RECONCILED (owner-adjudicated). The audit/phase-line "wire producers into pending_statement_stmts (the a21a6de inert landing pad); string operands leak" describes the ARCHIVED compiler — a21a6de is not an ancestor of HEAD, the field never existed in the fresh tree, and expression temporaries were ALREADY released per-statement via temp_owners + emit_temp_cleanup (string_concat/string_equality already valgrind-clean). The real two-mechanism split was: (1) the per-statement temp list (temp_owners, 8 producer forms) vs (2) the store-site release path needs_site_release_after_store (object/array/f-string/function-expr literals + object/function-typed match, released at let/assign/push/insert/property-set sites), the latter leaking fresh productions in non-store positions. RULING (AskUserQuestion): "unify onto the temp list." IMPLEMENTED: the 5 fresh-production forms now mint statement-scoped tracked temps in Temporary context (generate_expression interception + fresh_production_temp_kind, capturing the construction via an Owned-context recursion — the string-concat precedent generalized); the retaining store keeps its +1; needs_site_release_after_store and its 11 call sites were DELETED (releases relocate from store sites to statement end — refcount-neutral). This closed a real leak class (a fresh literal in a bare statement or as a borrowing call argument, covered by neither mechanism — pinned clean by temp_nonstore_object/array/arg). One more ad-hoc site fixed: the print-fstring inline release (would double-free a now-tracked fstring temp) collapsed to the regular path. Two DEAD, WRONG classification predicates deleted (expression_produces_heap_value, is_heap_allocating_expression — no callers, disagreed with reality; the finding the instruction asked to report). The dead scope_depth component of temp_owners/enclosing_temp_frames was dropped. DISCLOSED DEVIATION from the approved plan: temp_owners + enclosing_temp_frames were kept as the cached-top + unwind-stack form of the ONE mechanism rather than fused into a single vec — the two-field form is already the clean single-mechanism implementation and fusing adds error-prone break/continue/return index-shifting for zero behavioral gain. "The single cell-release function" is the codegen dispatcher emit_temp_release (there is NO value-dispatching runtime release; hl_cell_release is only the header primitive; runtime frozen this phase). ZERO-COST vs the 3e-γ binary: all program outputs byte-identical corpus-wide; C-diffs confined to the two declared classes (release-site placement + temp-declaration hoisting). [CORRECTED 2026-07-19 by the Phase-5 preflight re-derivation, docs/state-of-migration.md §A.2: the authoritative counts are 254 byte-identical C + 52 stable C-diffs + 1 nondeterministic (modules/diamond) of 307 compiling-both (333 entries, 26 rejection). The original "255 / 49 diff / 50 verified" undercounted and was internally inconsistent.]

Phase: (previous) Cell migration Phase 3e-γ COMPLETE — aliased-slot retarget attribution (origin-keyed node moves).
Status (3e-γ): Attribution is the semantics (ruling): each decl-form container-content subscription node records the SLOT CELL whose follow created it — HiLowCellWatcher.origin, pointer identity set at construction through the new hl_watcher_new_subscribed_origins (cell, modifier, origin) triples constructor, emitted only for watchers with followed variables; expression-form nodes, slot (changed)/(assigned) nodes, and FOLLOW markers carry NULL and never move. hl_slot_retarget collects only nodes whose origin is the rebinding slot's cell and removes them via the new origin-filtered hl_cell_unsubscribe_watcher_origin (one backref per removed node; other-origin nodes keep theirs); re-subscription preserves origin, which is constant across moves — so each slot's later rebind finds exactly its own nodes, resolving the FOLLOW-orphan follow-on with no extra machinery. hl_cell_subscribe gained the origin param (verified runtime-internal — never emitted by codegen; zero generated-C impact). Nothing name-keyed: origin is the same &slot->cell identity the FOLLOW marker uses. Two followed variables holding the same container are now independent: rebinding one leaves the other's subscriptions firing on the value it still holds (shape-2 ruling output pinned), and the moved-(deep) re-mark runs only for the rebinding slot's own nodes. Zero-cost vs the β binary: 295 of 300 compiling entries byte-identical; the declared diff class held EXACTLY — 9 programs (the 5 pre-existing decl-form container-content fixtures + the 4 new alias fixtures), each diffing only in the construction call (pairs → triples); 26 no-C-both = the rejection list; the plan's declared deviation (the diff class members are pre-existing corpus since β landed them) was adjudicated by plan approval. All program outputs byte-identical corpus-wide; no expected file edited.

Phase: (previous) Cell migration Phase 3e-β COMPLETE — decl-form content-following on containers (retargeting).
Status: Retargeting landed per the audit §5 item 10(b) step table, implemented as written: container set functions are compute-changed → store → hl_slot_retarget (for each HL_SLOT_FOLLOW marker node on the slot's cell, move that watcher's nodes from the old value's cell to the new value's cell in collected order — hl_cell_subscribe appends = item 9 fire order; moved (deep) nodes re-mark the new subtree) → fire (changed-then-assigned) → release old LAST. Retargeting is unconditional under pause (moves nodes, not watcher state) and stealth (the store happens; only notification is suppressed). Decl-form (changed)/(assigned) on containers are pure SLOT subscriptions per the spec ("mutating in place never fires a variable subscription") — the flipped sentinel's shape ((changed)xs + push) is pinned as printing nothing; content-following belongs to (added)/(removed)/(moved)/(deep). Soundness hardening (adjudicated with the plan — the α claim that the notify walk saves next before the body was refuted by the code): (A) collect-then-fire in hl_cell_notify's direct and per-ancestor loops; (B) hl_notify_depth + deferred old-payload release in the set family, drained at depth 0 (hl_cell_set_str shares the deferral — disclosed one-line extension beyond the plan's "untouched" note). Two latent α bugs found by probes and fixed: let-aliasing a slot-boxed variable's payload tracked the alias as HeapType::Scalar with no retain; the program-body pre-pass stored Type::Unknown for inferred container lets so nested functions couldn't key the slot setter (now falls back to codegen inference for slot-needing declarations only). Zero-cost vs the α binary: all 294 both-sides programs byte-identical, zero diffs; 6 new-only fixtures; 26 no-C-both = the rejection list.
Branch: main
Last commit: Phase 5c: shared scalars — atomic + cross-context watchable; 5a-ii graduated; closes Phase 5

Tests (2026-07-22, main, --no-fail-fast): ALL suites green — integration 339 passed / 0 failed / **1 ignored** (was 332/0/1: +5 shared fixtures, +2 scope-fence rejection tests); inbox_unit 2 passed (7 C-harness cases, incl. the two 5a-ii sole-owner-frees proofs); valgrind gate 1 passed, 275s (KNOWN_MEMORY_BUGS EMPTY; REJECTION_FIXTURES 26 → **28**: +shared_container_rejected, +shared_deep_rejected; the 5 shared fixtures ride the gate memcheck-clean); boxing_analysis 14; all other suites 0 failed. Byte-identical corpus VERIFIED vs bc51de4 (365 total, 5 diffs = exactly the 5 new shared fixtures; every existing single-threaded AND async program byte-identical; modules/diamond matched this run — its module-order nondeterminism is pre-existing, not a regression). Helgrind informational (NOT gated), stays non-gating through Phase 5.

Cell redesign: Phases 1, 1.5a–1.5e, 2a–2e, 3a–3e, 4a, **5a–5c complete — Phase 5 (queues + concurrency) CLOSED**. What the queue guarantees / does not: see state-of-migration §H.3 (same-thread synchronous, cross-thread coalescing MPSC inbox, atomic shared scalars + locked subscriber lists, atomic refcounts; NO global cross-producer order, coalescing on the cross-context path, shared containers + `(deep)`-across-shared + `(atomic-add)=` deferred). Next: Phase 6 — process tier (6a sendable check, 6b serialized transport, 6c process monitors; cell-migration-audit §Phase 6).

---

## Recent sessions

**2026-07-23 Phase 6c — crash robustness; PHASE 6 AND THE CELL MIGRATION CLOSED (COMPLETE)**: Per the ratified 6c mini-brief (inlined into docs/phase6-brief.md §3-6c, replacing the sketch); audit §6 not consulted. Step zero: baseline green at 2989bed (integration 343/0/1, both valgrind lanes, xproc 7, shm_unit 2). **NO COMPILER OR RUNTIME CODE CHANGED** — 6c asserts that what 6a/6b built already survives violent death, and the fixtures confirmed it on the first run with no fix required. The mini-brief itself found most of the sketch's scope already settled by 6a/6b decisions: cleanup policy was decided (c1 persistence is semantic; unlink-on-last-detach REJECTED as it would break the 6a persistence fixture), cleanup is explicit and out-of-language (c2), liveness-as-a-watchable deferred for want of a spec process-identity concept (c3), and the inbox-bypassing pull recorded as scalar-only soundness (c4). DELIVERED: three crash fixture sets, xproc harness 7 → 10 — crash_producer (SIGKILL mid-unbounded-write-loop; phase A a LIVE consumer verifies no delivered value ever goes backwards and exits on a fire-count FLOOR `fires>=20`, assertable under R3 unlike an exact count; phase B a fresh survivor proves the abandoned segment still verifies its header, still holds the last completed write, and does not wedge the pull — it watches, so every back-edge pulls against an epoch that will never advance); crash_consumer (attached consumer SIGKILLed 400ms into a 500000-write burst measured at ~1.5s under valgrind, so the kill provably lands mid-burst; producer completes and a fresh reader sees exactly 500000 — R-B no-queues/no-back-pressure made executable); crash_persist (violent death + normal exit, ZERO attachers for an interval, value 42 survives — ruling c1 made executable; under unlink-on-last-detach this prints 0). Harness gained native_child (victims are NOT valgrinded — a SIGKILLed process never reaches ERROR SUMMARY so its report cannot be evaluated; every SURVIVOR still is) and kill_and_reap, which ASSERTS the victim was still running before the kill, so a victim that exited early fails loudly instead of passing vacuously. DESIGN CORRECTION (§K.2 6c-iv): case 1 first used a fixed-iteration observation window sized to outlive the kill; that is unworkable because idle_polls backoff is cumulative, so post-death each iteration sleeps ~1ms — a window large enough to survive the live phase ran ~30 minutes once the producer died (caught as a hang, diagnosed, restructured). The tension is intrinsic (iteration cost differs ~100× between peer-alive and peer-dead), so it was replaced with the two deterministic phases above and NO sleeps in case 1 — each fixture's exit condition sequences the test. SPEC: watcher-body idempotency guidance in Cross-Process Watchers (at-least-once means a body may run more times than there were changes; the 6b threshold double-fire is the worked example, both fixes shown — `onCounter.end()` and a guard flag) + a Segment Lifetime and Cleanup subsection per c2 (`/hilow.<name>` flat per-user namespace, 0600, why segments persist, ls/rm on /dev/shm, that unlinking a LIVE segment silently splits its users into two groups, no reboot survival). Cleanup tooling decided docs-only (decide-and-disclose, §K.2 6c-ii): on Linux the whole operation is ls/rm, so a helper script would add an unversioned utility and a second way to name segments for no capability gain. **SWEEP NOT PERFORMED — STOP-AND-REPORT**: the mini-brief's C-style-`for` sweep prescribed converting occurrences "to iterator form", but premise-checking showed that target form is ITSELF unimplemented — the parser accepts only `for (let (i,v) in expr)`, so 18 of the spec's 23 `for` loops do not compile, and the spec's Loops section documents both unimplemented forms as features. Resolving it means either implementing two loop forms (compiler work, own phase) or removing two documented forms from the language surface — an owner ruling, not a doc sweep. Nothing changed pending it; see Open questions. Full ritual green (literal output in the debrief). REJECTION_FIXTURES 32; ignored 1; KNOWN_MEMORY_BUGS empty. Phase 6 closure (guarantees vs non-guarantees) §K.3; adjudications §K.2; deferrals with real preconditions in brief §6. CLAUDE.md phase line → NONE (migration complete; owner direction required — the audit has no successor to Phase 6).

**2026-07-23 Phase 6b — cross-process DELIVERY (COMPLETE)**: Per docs/phase6-brief.md §3-6b; audit §6 not consulted. Step zero: baseline green at 6c530a8; verified the 6a write protocol intact (single release-ordered payload+epoch publication) — no stale premise. DELIVERED: (runtime) HiLowThreadContext.placed_watches record `{cell(retained), last_seen_epoch, last_delivered}` + idle_polls; hl_shm_epoch (acquire), hl_shm_exchange_i32 now returns the new epoch; hl_placed_watch_ensure (on subscribe, seeds to current), hl_thread_pull_placed (safe points + backoff: yield×16 then nanosleep≤1ms), hl_thread_pull_placed_once (residual at final drain), hl_placed_clear. The pull UNIFIES cross-thread-local + cross-process: hl_notify_deliver skips the inbox for a placed cell to a non-declaring thread (owner!=self) → that thread's pull delivers (multi-thread-watching correct for free); hl_inbox_fire_entry untouched. DEDUP: same-thread write fires synchronously (R1) then advances its record to the epoch it produced → pull skips (monotone epoch ⇒ never skips a real remote write). (codegen) emit_loop_safepoint + exit hl_thread_final_drain now also fire under uses_placement (a placement-only consumer spins on a threshold with no allocation; back-edge is where its pull runs) — join stays async-only; existing corpus byte-identical. SPEC: the Cross-Process Watchers example became a real 2-program fixture; spec text edited (named shared("counter")) AND — FLAGGED DEVIATION — its C-style `for (let i=0; i<100; i+=1)` changed to `while` because HiLow's `for` is iterator-form only, so the example never compiled (pre-existing spec discrepancy, independent of the one-token edit the brief anticipated; only this one loop touched). LIVE FINDING validating the brief: an un-guarded threshold fixture printed "Done!" TWICE under one interleaving (the loop-pull observed an intermediate value and fired (changed); the body read the current value, already past threshold; then the residual final-drain pull fired again) — exactly "fire count ≠ remote-write count". Fixed by making the announcement idempotent (guard flag; the spec shows onCounter.end()). Fixtures: xproc_harness 7 (share×2, dedup_same [3 same-thread writes → 3 (changed), pull adds nothing — R1], dedup_cross [async writer delivered by pull, no inbox], prodcons [remote write → consumer's watcher via pull], watcher_exits [producer unaffected by consumer leaving — no coupling], spec_example [Done!]) all under valgrind both processes, stress-run 8× clean; shm_unit 7 C cases (+ pull changed/assigned + ABA pinned deterministically via a poke hook, HL_SHM_TEST_SUPPORT-only). BYTE-IDENTICAL: 32 at-risk async/shared/loop programs, 0 diffs vs 6c530a8. Full ritual green: integration 343/0/1, both valgrind lanes 2, xproc 7, shm_unit 2, boxing 14, inbox 2; KNOWN_MEMORY_BUGS empty; ignored 1; REJECTION_FIXTURES 32. Housekeeping rider: empty-string-literal lexer defect (found 6a) recorded in STATUS code-quality (destination: unscheduled lexer fix). Adjudications §J.2. CLAUDE.md phase → 6c (lifecycle mini-brief).

**2026-07-23 Phase 6a — cross-process `shared` by placement (COMPLETE)**: Per docs/phase6-brief.md §3-6a (rulings R-A–R-E); audit §6 not consulted. Step zero: baseline 343/0/1 + both valgrind lanes green at 84f707b; verified the brief's §1 ground truth against the 5c machinery (sub_lock/kind/accessors) — no stale premises. DELIVERED (layer by layer, each verified): (1) runtime shm — HiLowCell.shm, versioned header {magic,layout,abi,type_tag,payload_size,init_state,epoch}, hl_shm_attach_i32 create-or-attach per R-D (O_CREAT|O_EXCL winner inits + release-publishes; loser bounded-waits + verifies every field + observes/fires-nothing; startup-error exit(1) with diagnostic on mismatch/timeout), hl_scalar_new_i32_placed (⊃ _shared), accessor 3rd branch (mapped-slot acquire-load / exchange+epoch-bump), teardown munmap+close (persistent, no unlink); (2) parser/AST shared_segment: Option<String>, `shared("name") let`; (3) codegen placeability fence (non-i32 placed → pointer/watcher-bearing diagnostics) + name validation + placed ctor emission + uses_placement→-lrt; (4) 4 rejection fixtures (container, watcher, bad-char, over-64-length) + is_err tests, REJECTION_FIXTURES 28→32; (5) two-process harness tests/xproc_harness.rs (both orderings, valgrind-clean, unlinks its segment); (6) C-fork unit harness tests/shm_unit.rs (5 cases — the type/magic-mismatch + timeout + empty-name paths unreachable from HiLow, proven via forged segment under -DHL_SHM_TEST_SUPPORT, zero production footprint). BYTE-IDENTICAL corpus VERIFIED empirically: built 84f707b vs HEAD binaries, dumped generated main.c for all 365 programs (via a forced-link-failure that skips temp cleanup), 322 both-compiled, 0 diffs — matches the structural proof (every change gated on placement). Adjudications recorded in state-of-migration §I.2 (fence-in-codegen; placed⊃shared; harness = gate-equivalent-rigor-in-a-dedicated-test; mismatch/timeout tested at C level; attacher-ignores-init; persistent-by-default). FINDING: empty string literals do not lex in HiLow (pre-existing "Unterminated string"), so `shared("")` is caught at the lexer — the codegen empty-name branch is unreachable from source (pinned at C level instead; the 65-char length rule IS source-reachable, pinned by placed_long_name_rejected). Full ritual green: integration 343/0/1, both valgrind lanes pass (this run 205s under my own parallel-valgrind contention — the lane split clocked 23–38s earlier this session; the added non-placed `if(shm)` branch is predictable-NULL), shm_unit 2, xproc_harness 2, boxing_analysis 14; KNOWN_MEMORY_BUGS empty; ignored 1. CLAUDE.md phase line → 6b. Parked questions (shared-in-6a, boundary-locality) resolved by R-A (per-declaration placeability fence, no send ⇒ no boundary).

**2026-07-23 Phase 6a opening commit — delete test_phase7a_integration (test infra only)**: Per phase6-brief §3-6a opening commit (separate, before phase work). Deleted the `#[ignore]`d `test_phase7a_integration` from tests/integration_phase7a.rs (a deletion-marker comment left in its place records why); dropped the now-unused `CodeGenerator` import (the two surviving tests use Parser+TypeChecker only). Before deleting, VERIFIED the brief's proviso ("add a fixture if object_* leaves hl_object_get_str uncovered"): object_mixed_types.hl reads `person.name` and prints "Alice" under a live end-to-end integration test (test_object_mixed_types_integration) — that covers the string-property-read codegen path superiorly to the deleted substring assertion, so **no replacement fixture was needed**. Ignored ledger 2 → **1** (unknown-with-options → 9b only). No compiler change; existing corpus untouched. Ritual green. NOT started: 6a phase work proper (grammar/AST/shm/harness) — next session, fresh context.

**2026-07-23 Phase 6 brief ratified (`docs/phase6-brief.md`; docs-only)**: The "chat brief in progress" resolved. Persisted the ratified Phase 6 brief verbatim as `docs/phase6-brief.md` (rulings R-A–R-E) and wired pointers: CLAUDE.md phase line → 6a governed by the brief; CLAUDE.md Sources-of-truth §3 gained a Phase-6 exception (audit §6 is not a plan; the brief governs); audit §6 SUPERSEDED note now points at the brief; STATUS current-state rewritten. Ground truth verified against the tree (matches the 6a step-zero recon — no stale premises). Key rulings: cross-process `shared` by **placement** in named shm segments, NOT transport/serialization (retired); surface `shared("name") let` (bare `shared` unchanged = 5c cross-thread); the sendable check becomes three **placeability** rejection diagnostics (non-scalar = not placeable), not a phase — which also resolves the two parked recon questions (per-declaration fence, not transitive; no send ⇒ no laundering boundary). Sub-phases: 6a placement+attachment+two-process harness, 6b delivery (epoch-pull at drain safe points, R3), 6c lifecycle (sketch, mini-brief after 6b — the pid-as-watchable-cell payoff). 6a to open with a separate commit deleting `test_phase7a_integration` (ledger 2 → 1). No code changed this commit; ritual not re-run (docs-only; prior maintenance commit's ritual green at 5136008).

**2026-07-23 Phase 6 pre-work maintenance commit (docs + test infra only; no compiler changes)**: A standalone maintenance commit ahead of the Phase 6 re-scope. Preceded by an aborted 6a reconnaissance that produced two owner rulings — surface question (b) (spec's process model stands; no spawn construct; the sendable check already exists as 5c's scope fence, so standalone 6a dissolves → Phase 6 re-scoped via chat brief; do not implement audit §6), and the ignored-ledger policy (document honestly; the invariant is "every ignore named, reasoned, destined," not a count). Three work items: **(m1') Ledger repair** — STATUS documented-ignores rewritten to the TRUE ledger (2, in two files): `test_unknown_with_options_integration` (→9b) and `test_phase7a_integration` (integration_phase7a.rs; origin 186bc9c, reason quoted). Removed the stale `test_watcher_reentrant_deferred_integration` reference — that test was activated+renamed `watcher_reentrant_sync` in 5b (the STATUS claim "all in integration_tests.rs; there are 2" was stale in both directions). Read-only sizing of the phase7a ignore: the ignore reason ("get_expression_type needs symbol table context") is STALE — the program compiles+runs end-to-end today (exit 0); the commit that added the ignore also added the superseding end-to-end tests (object_basic/assign/mixed_types); the test now fails only because its assertions demand output the source never generates (`hl_object_get_str` — never reads a string property; `print_i32`/`print_str` — never prints). Destination: unscheduled, TEST-ONLY (delete as superseded, or rewrite assertions + add a read/print), blast radius zero-compiler. **(m2) Valgrind gate lane-split** — one `valgrind_gate` test → `valgrind_gate_single_threaded` + `valgrind_gate_threaded`, routed by source-token detection (`is_threaded_source`: async/shared, comment-stripped) so new threaded fixtures auto-route. Threaded lane runs under `--fair-sched=yes` (the DOMINANT fix — without it valgrind's serialized scheduler starves a busy-wait producer for 60–120s; with it, sub-second even at full N) and compiles the two spin-wait fixtures (shared_reactive_counter, async_watch_threshold) at a reduced **N (env `HILOW_GATE_SPIN_N`, default 4)** injected by gate-side source rewrite (SPIN_FIXTURES table; per-substring honesty check fails on fixture drift; pristine .hl at full N is what the integration tests compile). N is safe for any N≥1 (invariant is memory-cleanliness, N-independent + order-insensitive). MEASURED: single-threaded lane 33.9s (was folded in the 275–763s single test — back near pre-Phase-5), threaded lane 1.57s. **(m3) Doc-truth sweep** — audit §5b annotated DESCOPED (spawn/pid/write-list never built; real 5b = minimal async, see §F), audit §6 annotated SUPERSEDED-pending-re-brief, `concurrency-design.md` header-marked HISTORICAL/NON-CANONICAL (write-list model never implemented). Added a standing practice to Methodology lessons: "when a phase descopes a deliverable, sweep its downstream consumers in the same commit." CLAUDE.md phase line → "6 — re-scoping (chat brief in progress)". Ritual green (both lanes); ledger 2; KNOWN_MEMORY_BUGS empty; REJECTION_FIXTURES unchanged (28).

**2026-07-22 Cell migration Phase 5c — `shared` scalars (closes Phase 5)**: Per the adjudicated brief §5c + the two prompt amendments (per-cell subscriber lock, 5a-ii graduation — both recorded in state-of-migration §G BEFORE implementation). STEP ZERO (two parts): (0a) baseline 332/0/1 + gate + inbox_unit green, no drift since bc51de4; (0b) FIXED deviation 5a-ii FIRST as its own C-harness-proven change on a closed boundary — `HiLowCell` gained a `kind` tag (set at all 4 constructors), each typed release's teardown factored into `hl_scalar/array/object_finalize`, new `hl_cell_release_full` dispatches on `kind` at refcount 0, the inbox (`hl_inbox_fire_entry`) routes through it; two harness cases (`case_inbox_sole_owner_frees_{array,scalar}`) prove the inbox-as-sole-owner-at-drain frees the typed container (each LEAKS 152B with the pre-fix `hl_cell_release`, valgrind-clean with the fix); blast radius stayed within the release path (+ construction-time tag), so proceeded. IMPLEMENTED (shared): (1) `LetDecl.is_shared`, `shared let` parsed via a `TokenKind::Shared` dispatch; (2) boxing marks shared lets boxed (`BoxReason::Shared`); (3) runtime `HiLowCell.sub_lock` (`pthread_mutex_t*`, non-NULL ⟺ shared; `hl_scalar_new_i32_shared` allocates, header teardown frees) — atomic payload via a branch in `hl_scalar_get_i32`/`hl_cell_set_i32` (out-of-line calls, so no register hoist; non-shared byte-identical), and the per-cell subscriber lock in `hl_cell_subscribe`/`unsubscribe[_origin]` + a snapshot-under-lock-then-fire-unlocked in `hl_cell_notify` (shared sets skip the racy `cell_has_audience` pre-check); (4) codegen `uses_shared`→`threaded_mode()` (= async || shared), `shared_vars` in the pre-pass, `_shared` constructor at the boxed-i32 let, `-DHILOW_THREADED` keyed on `threaded_mode()`; (5) `pthread.h` added to runtime.h (pointer field; main.c source byte-identical). SCOPE FENCE: shared containers → codegen diagnostic (`shared_container_rejected`); `(deep)` across shared → subsumed by the typecheck deep-requires-container rule (shared is scalar-only, so deep-on-shared = deep-on-scalar; a dedicated codegen check would be unreachable dead code — removed; pinned by `shared_deep_rejected`). REJECTION_FIXTURES 26 → 28. SPEC R4: `docs/hilow-design.md` `shared` unified to one meaning (refcount + atomic + cross-context watchable). FIXTURE-1 DESIGN NOTE: with racy RMW, workers loop `while (counter < N) counter += 1` (monotone reach of N is race-tolerant; a fixed count could fall short); atomic accessors are genuinely needed (the out-of-line call already prevents register hoist, but atomics give cross-thread visibility + defined behavior). Five order-insensitive fixtures (reactive-counter-under-contention, watch-declared-mid-production [proves the subscriber lock; `stop` also shared for reliable termination], ended-watcher-on-locked-cell, exit-release, shared-without-async), each blocking main on a MAIN-THREAD flag → deterministic; stress-run clean; all valgrind memcheck-clean via the gate. BYTE-IDENTICAL vs bc51de4: 365 total, 5 diffs = exactly the 5 new shared fixtures (all existing single-threaded + async programs identical; diamond matched this run). Full ritual green: integration 339/0/1, valgrind gate 1 (275s), inbox_unit 2 (7 cases), boxing_analysis 14; KNOWN_MEMORY_BUGS EMPTY; ignored stays 1. 5a-ii moved to extinct (§D.2). Phase 5 CLOSED (§H.3 closure summary). CLAUDE.md phase line → 6a (process tier / sendable check).

**2026-07-21 Cell migration Phase 5b — minimal `async`**: Per the adjudicated Phase-5 brief (§5b) + the amendment. STEP ZERO (green): re-verified baseline 326/0/2 + valgrind gate + inbox_unit; confirmed no drift in the 5a routing path since 1c3bd2f (`git diff` empty on runtime.{c,h}). RESOLVED CONFLICT (AskUserQuestion — the 5b re-entrancy statement says same-thread firing is SYNCHRONOUS/"existing semantics unchanged" (R1), but the committed `watcher_reentrant_deferred` expected file pinned a DEFERRED same-thread output; the authoritative brief `cell-redesign-brief.md:63` permits EITHER): owner chose SYNCHRONOUS → the inbox is the cross-thread path exclusively; the fixture is activated with its expected output rewritten to the synchronous/nested result (which the current binary already produces) and renamed `watcher_reentrant_sync`. AMENDMENT (recorded in the brief + state-of-migration §E.5 BEFORE implementation): drain safe points gain a threaded-mode-only codegen-emitted loop-backedge check (a runtime-only safe point never fires in a pure-compute `loop { }`). IMPLEMENTED: (1) `Statement::Async` (parsed like `stealth`; typecheck arms mirror StealthBlock — mode unenforced project-wide, so no Low-mode gate); (2) boxing `walk_async` capture boundary (async-referenced outer vars box → reachable as heap cells); (3) codegen `generate_async_block` — body → `void* fn(void*)` pthread entry into a new `async_bodies` buffer (before main), captures packed into a heap env (retained; `emit_watcher_env_struct` reused) passed to `hl_async_spawn`, body ends `dtor(env); free(env); hl_thread_final_drain(); return NULL`; spawn at the statement site; (4) runtime `hl_async_spawn`/`hl_async_join_all` (growable thread list, mutex-guarded) + `HL_RC_INC`/`HL_RC_DEC` atomic-under-`HILOW_THREADED` macros applied to all 8 refcount owners + 3 ref helpers (object/array/scalar funnel through `hl_cell_retain/release`) + `hl_alloc_count`/`hl_free_count` → `_Atomic int` in threaded mode (so the UNCHANGED generated `hl_alloc_count++` bytes compile atomic; `_Atomic` is a C11 keyword, no stdatomic.h in main.c); (5) threaded-mode detection `program_body_has_async` (pre-scan, so a loop preceding the async block still gets its safe point) → gates loop-backedge `hl_thread_safepoint()`, the exit join/drain (BOTH the explicit-`return` path and the fall-through epilogue — before scope cleanup so residual fires see live cells), and `-DHILOW_THREADED` (added in main.rs, applied to both TUs in one `cc`). BYTE-IDENTICAL: every threaded-mode emission gated on `uses_async`, so a non-async program emits identical C — VERIFIED by full-corpus C-diff vs the 1c3bd2f worktree binary: 352/352 successfully-compiling single-threaded programs byte-identical; the only diffs are the 5 new async fixtures (new programs) and `modules/diamond` (the pre-existing module-emission-order nondeterminism — the new binary produces BOTH md5 variants run-to-run, one byte-matching base, 0 async artifacts in its C). Two capture-race false positives in an earlier diff run (foreground fixture builds racing the background diff's `ls -dt /tmp/hilow_*`) were traced to the STATUS "never run the compiler concurrently with the gate" hazard and discarded; the clean re-run is authoritative. FIXTURES (+5, each blocking main on a MAIN-THREAD-updated flag so output is scheduling-independent; each stress-run 30–50× deterministic, all valgrind memcheck-clean via the gate): async_watch_threshold (enqueue+backedge-drain+retention), async_multi_producer (MPSC, conservation seen==3), async_added_accumulate (R3 accumulation, count==5 sum==15), async_ended_watcher (R5 ended-drops, live co-subscriber still fires), async_exit_join (in-flight entry delivered by exit join-then-drain). RE-ENTRANCY test activated (ignored 2 → 1). Helgrind lane informational (0 errors, standard suppressions). Full ritual green: integration 332/0/1, valgrind gate 1, inbox_unit 2, boxing_analysis 14; KNOWN_MEMORY_BUGS EMPTY; REJECTION_FIXTURES 26. CLAUDE.md phase line → 5c. DEVIATIONS/notes recorded in state-of-migration §F.

**2026-07-21 Cell migration Phase 5a — notification-queue infrastructure (no language surface)**: Per the adjudicated Phase-5 brief (§3 5a). STEP ZERO (brief §5): re-verified the preflight facts against the tree — baseline 326/0/2 + valgrind gate green; delivery still fully synchronous inline in `hl_cell_notify` (body_fn called directly under the `hl_notify_depth` guard); `hl_deferred_releases` still a same-thread release deferral (distinct mechanism); all four statics present as described (`hl_stealth_depth` global/48, `hl_deep_epoch` static/53, `hl_notify_depth` static/1050, `hl_deferred_releases` static/1057). No drift. RESOLVED FORK (AskUserQuestion — the CLAUDE.md phase line "allocation/syscall boundaries" vs the brief §5a "statement boundaries" genuinely disagreed, and statement-boundary drains would require codegen-emitted `hl_safepoint()` calls = a program-logic codegen diff that 5a's own expected-diff forbids): owner chose RUNTIME safe points → NO codegen change, program-logic codegen byte-identical. IMPLEMENTED (runtime.c/.h + one main.rs flag; ZERO `codegen/mod.rs` change): (1) the four statics → `_Thread_local` (in place; `hl_stealth_depth` stays a named `extern _Thread_local` global because generated C emits `hl_stealth_depth++/--`); (2) `HiLowThreadContext` (mutex-guarded global registry, lazily created per thread via `hl_current_ctx`) holding an MPSC mutex-guarded `HiLowInbox` (entries keyed by watcher identity; `atomic_int nonempty` flag); (3) coalescing enqueue `hl_inbox_enqueue` — merge on a watcher already pending, accumulating a per-entry delta list (R3: bare (changed)/(assigned)→pending flag; (added)/(removed)/(moved)→appended, delivered in order at drain via the existing one-delta body ABI); retains the watcher (R6) + the cell (axiom 5), takes delta ownership; (4) `HiLowWatcher.owner_ctx` set at `hl_watcher_new`; `hl_cell_notify`'s two fire sites route through `hl_notify_deliver(self, …)` — `owner==self` → the existing synchronous fire (byte-identical), else enqueue (delta copied via `hl_delta_copy_for_queue` since the notify delta is borrowed); (5) drain `hl_thread_drain_inbox` detaches under lock then fires outside it (a body may re-enqueue without deadlock), ended watchers drop WITHOUT firing (R5), legal only at depth 0; `hl_thread_safepoint` (cheap nonempty test, never creates a context) wired at `hl_object_new`/`hl_array_new` (alloc) + `print_str` (syscall) — DORMANT single-threaded; `hl_thread_final_drain` (axiom 6) for teardown; (6) `cc` gained `-pthread`. PROOF (brief §5a — only one thread exists, so the queue path has no HiLow surface): C-level unit tests, tests/inbox_unit_harness.c (5 cases: enqueue+drain fires once; coalesce accumulates BOTH deltas under ONE entry with ONE watcher retain; distinct watchers don't coalesce; ended watcher drops without firing but still releases refs; final-drain empties) driven by tests/inbox_unit.rs (compiles the harness against runtime.c with `cc -pthread`, runs it, then re-runs under valgrind asserting 0 definite/indirect + 0 errors). Bug caught by the harness's own valgrind lane: `hl_cell_release` at refcount 0 does only cell-header teardown (the typed container release frees the struct), so the harness must free arrays via `hl_array_release` and must not make the inbox the container's SOLE owner — which surfaced DEVIATION (2) below (a real 5c boundary). DEVIATIONS (state-of-migration §5a): (1) the four statics are `_Thread_local` file-scope globals, not `HiLowThreadContext` fields — the per-thread invariant holds and `hl_stealth_depth` must stay a named global for byte-identical codegen; the context holds the inbox+registry (the parts needing cross-thread discovery). (2) The inbox owns `+1` on the cell but `hl_cell_release`-at-0 can't free a typed container, so the inbox must not be the cell's sole owner at drain — UNREACHABLE in single-threaded 5a (inbox dormant), resolved in 5c (cross-thread cells are typed `shared` scalars → the entry will carry the scalar's typed release). (3) The entry snapshots `body_fn`/`env`/`cell`/`event` beyond the brief's `(watcher, delta, pending)` triple — the fire needs them. ORDERING non-guarantee documented (brief §5a): per drain pass one cell's watchers fire in declaration order and one producer's entries are observed consistent with its mutation order under coalescing; there is NO global order across producer threads. Single-threaded corpus byte-identical (326/0/2; program-logic codegen untouched); valgrind gate green; inbox_unit 2/2 valgrind-clean; KNOWN_MEMORY_BUGS EMPTY; REJECTION_FIXTURES 26; ignored stays 2. CLAUDE.md phase line → 5b.

**2026-07-20 Phase 4b — extinguish the match-arm ownership use-after-free (4-series closeout)**: Fixed exactly the preflight's Known Bugs entry; nothing else. Baseline verified clean (326→ was 321/0/2 pre-fixtures, gate green). DIAGNOSIS (under-retain, not over-release): a `match` whose result type is a heap type other than Object/Function, with a BORROWED arm (Ident/member/index) consumed in Owned context, handed a bare borrow into Owned — but the let-match `track_heap_owner` arm (mod.rs:1545) records the binding as an Array owner, so the scope-exit `hl_array_release` over-freed the borrowed source (double-free / UAF in `hl_cell_release`). The 4a axiom — Owned context receives a +1 the consumer drops; borrows never cross into Owned unretained — was violated because `generate_match_expression`'s `ref_wrap` (mod.rs:7060) only retained borrowed arms for Object/Function. REACHABILITY bounded by probe (predictions first, all matched): String and DynamicArray = the live bug (11 valgrind errors each); Optional-typed match is CLEAN (not tracked as an owner at 1545 → never over-releases); Unknown is not constructible as a bare arm (parse error); Object/Function already retained. FIX (class-level, retains at the arm): extended `ref_wrap` to `Type::String | Type::DynamicArray(_) if Owned && expr_is_borrowed_ref → hl_array_ref`, mirroring the Object/Function case; Temporary context deliberately NOT wrapped (already correct — bare-statement and borrowing-argument variants were probed clean before and after, so a wrap there would double-retain). No runtime change (hl_array_ref exists); no new helpers (Optional/Unknown out of the reachable class). DECLARED DIFF CLASS (stated before the ritual): retain-site insertions (`hl_array_ref(...)` around borrowed string/array arms in Owned-context match) — ZERO-COST confirmed: corpus C-diff vs 73a8a3a = 306 byte-identical, 0 real diffs, 1 nondeterministic (modules/diamond), 26 rejection; the fix touches no existing program (the bug was latent/untested). CENSUS (post-4b, one sentence): the temporary census is unchanged — `fresh_production_temp_kind` still mints only Object/Function-typed match (∪ the 8 self-minting arms) as a tracked temp, and every other heap-typed match (String/DynamicArray) is ownership-accounted at the arm (borrowed arms retained via hl_array_ref in Owned; fresh-literal arms self-mint a statement-end-released temp; Temporary-context borrows stay untracked), so match in ALL result types is accounted without routing through the temp mechanism. TESTS: +5 fixtures (2 fix targets Owned string/array borrow, 3 controls: Temporary bare, Temporary arg, fresh-literal Owned), every output predicted before running, all matched, all valgrind 0 — integration 321→326. KNOWN_MEMORY_BUGS stays EMPTY (fixed in-phase; the fixtures ride the normal gate, no listing needed); REJECTION_FIXTURES unchanged 26; ignored stays 2. Preflight Known Bugs entry moved to extinct classes (state-of-migration.md §D.2). CLAUDE.md phase line stays 5a (4-series closeout, not a new phase). Full ritual green.

**2026-07-19 Phase 5 preflight — read-only reconnaissance (docs-only commit)**: Produced `docs/state-of-migration.md`; no code changed. Baseline verified clean (integration 321/0/2, valgrind gate green) before starting. PART A (seal 4a): (A.1) the producer inventory `fresh_production_temp_kind` is a provable projection of the DELETED `needs_site_release_after_store` (the two predicates the 4a plan named as the census — `expression_produces_heap_value`, `is_heap_allocating_expression` — were dead+wrong and never the census; post-4a `fresh_production_temp_kind` ∪ the 8 self-minting arms IS the census). ONE value-producing site falls outside it, PREDATING 4a: non-obj/fn-typed `match` with a borrowed arm in Owned context — probed to a CONFIRMED use-after-free under both b4c78a5 and ced77dd (control fresh-literal-arm case valgrind-clean); recorded in Known Bugs, not fixed (read-only). (A.2) Re-derived the 49-vs-50 count authoritatively (main.c regenerated for every entry under ced77dd vs b4c78a5, double-captured to screen modules/diamond nondeterminism): **254 byte-identical C + 52 stable C-diffs + 1 nondeterministic (modules/diamond) of 307 compiling-both; 333 entries, 26 rejection (= REJECTION_FIXTURES, exact validation)**. The 4a "255/49/50" undercounted and was inconsistent; corrected in both the Current-state and 4a-session blocks above. Three spot-checked diffs confirmed in the two declared classes (hoisting + release-site placement). PART B (delivery machinery): delivery is fully SYNCHRONOUS and inline — NO queue; every mutator does hl_delta_new_* → hl_cell_notify → hl_delta_release in one call; deltas are queue-ABLE (self-owning) but not queued; the 3e-β collect-then-fire snapshot and hl_notify_depth+hl_deferred_releases old-payload deferral are same-thread mechanisms, NOT the brief's notification queue; ZERO threading primitives in the runtime (thread awareness = none, confirmed); process-global mutable statics = hl_stealth_depth (flagged for TLS) + hl_deep_epoch + hl_notify_depth + hl_deferred_releases (UNFLAGGED) + hl_alloc/free_count, plus emitted `static HiLowScalar*` program-scope boxed cells. Reconciled 7 brief claims: 2 already-fixed ahead of Phase 5 (static delta buffers deleted in 2c; the per-statement release list done in 4a), 3 unbuilt (per-thread queue, safe-point drain, queue-or-sync choice), 1 stale terminology ("single cell-release function" = codegen emit_temp_release, not a runtime fn), 1 accurate (codegen injects no firing). PART C (enqueue-ownership audit): NO borrow of a statement-scoped temp is held at any enqueue/defer site — the one deferring site (hl_release_or_defer ×3) holds the slot's OWNED old-payload +1; deltas self-own; node snapshots are intra-call. Forward constraint: a Phase-5 notification queue must RETAIN the queued watcher (env/body_fn validity past the producing call). The STATUS "statement-boundary/drain independence" claim (marked UNVERIFIED) HOLDS for the synchronous tree but is independence of compile-time temp-frame nesting only, not a guarantee that queued delivery needs no ownership work. No STATUS/spec/plan re-litigation; CLAUDE.md phase line unchanged (stays 5a — this was preparation, not 5a).

**2026-07-19 Cell migration Phase 4a: statement temporaries — unify onto one per-statement release mechanism**: Owner-adjudicated premise reconciliation + the unification. STEP ZERO (green): re-ran the four 3e-γ attribution tests and an adversarial probe (one watcher following xs, alias ys rebound away — follower stayed put: `w: 2 / w: 3 / done`, valgrind clean; a name/cell-only attribution would have moved it), confirming attribution is by origin slot-cell identity, not the name path. PREMISE: the audit/phase-line 4a (wire producers into the a21a6de `pending_statement_stmts` landing pad; string operands leak) describes the ARCHIVED compiler — a21a6de is not an ancestor of HEAD, the field never existed in the fresh tree, temporaries were already released per-statement (temp_owners + emit_temp_cleanup; string_concat/string_equality already clean), and there is no value-dispatching runtime release (hl_cell_release is only the header primitive). Reported via AskUserQuestion; the real two-mechanism split was per-statement temp list vs store-site release (needs_site_release_after_store). RULING: "unify onto the temp list." IMPLEMENTED: (1) a generate_expression interception mints the 5 fresh-production forms (ObjectLiteral→Object, ArrayLit→Array, FString→Array, FunctionExpr→Function, Match(obj|fn)→Object|Function) as statement-scoped tracked temps in Temporary context, capturing the construction via an Owned-context recursion (fresh_production_temp_kind classifier; the string-concat precedent generalized); (2) needs_site_release_after_store + its 11 call sites DELETED (releases relocate from store sites to statement end — refcount-neutral, the store retains); (3) the print-fstring inline-release site collapsed to the regular path (it would double-free a now-tracked fstring temp — the one regression the initial fold-in caused, 16 tests, fixed); (4) two DEAD+WRONG classification predicates deleted (expression_produces_heap_value, is_heap_allocating_expression — no callers, disagreed with reality — the finding the instruction asked to report); (5) dead scope_depth dropped from temp_owners/enclosing_temp_frames. REAL LEAK CLASS CLOSED: a fresh literal in a bare statement or as a borrowing call argument (covered by neither mechanism) leaked — probed under the pre-change binary (bare object 236 bytes, bare array 100 bytes, array-arg 100 bytes, all valgrind-dirty), now clean; pinned by temp_nonstore_object/array/arg (+3 integration → 321). DISCLOSED DEVIATION from the approved plan: temp_owners + enclosing_temp_frames kept as cached-top + unwind-stack (the clean single-mechanism form) rather than fused into one vec — fusing adds error-prone break/continue/return index-shifting for zero behavioral gain. RE-ENTRANCY: generate_statement pushes/pops one frame per statement (balanced, underflow-guarded); nested statements get their own top frame; watcher bodies are separate C functions with a reset stack — unchanged, and independent of Phase 5 queue draining. ZERO-COST vs the 3e-γ binary: C-diffs confined to the two declared classes (release-site placement + temp-declaration hoisting), spot-checked across all form types; all program outputs byte-identical corpus-wide. [COUNT CORRECTED 2026-07-19 by the Phase-5 preflight (docs/state-of-migration.md §A.2): the authoritative counts are 254 byte-identical C + 52 stable C-diffs + 1 nondeterministic (modules/diamond, excluded — it diffs against itself) of 307 compiling-both; 333 entries, 26 rejection (= REJECTION_FIXTURES). The original "255 byte-identical / 49 diff / 50 verified" was an undercount and internally inconsistent (49 vs 50); most likely 49 excluded the 3 new temp_nonstore_* fixtures, 49+3=52.] No new compiler warnings (pre-existing set line-shifted). Full ritual green (integration 321/0/2, gate green); string_concat/string_equality valgrind-clean through the unified mechanism (the string-operand question closed). KNOWN_MEMORY_BUGS EMPTY; REJECTION_FIXTURES 26; ignored stays 2.

**2026-07-19 Cell migration Phase 3e-γ: aliased-slot retarget attribution — origin-keyed node moves**: Owner-ruled repair session interposed before 4a, per the step-zero instruction: probe the aliased-slot edge recorded at 3e-β; behavior did NOT match the requirement (STOP reported; owner ruled attribution is the semantics; approved plan adjudicated the rest). STEP-ZERO PROBES (β binary, predictions stated first, all matched, all valgrind 0): shape 1 (two watchers, one following each of two aliased variables) already correct — rebinding xs moves only its follower's nodes; shape 2 (ONE watcher following both variables, the recorded edge) wrong — ys.push after xs's rebind silent, xs.push fired twice, and ys's FOLLOW marker orphaned; aliased-(deep) probed REPRESENTABLE with the same defect. MECHANISM: HiLowCellWatcher.origin = the slot cell whose follow created the node (pointer identity — nothing name-keyed; the same &slot->cell expression the FOLLOW marker uses), set at construction via new hl_watcher_new_subscribed_origins (cell, modifier, origin) triples varargs, emitted ONLY when the watcher has followed variables (pairs symbol + emission untouched otherwise — verified byte-identical); NULL origin for expression-form/slot/FOLLOW nodes, never moved. hl_slot_retarget filter: watcher == w AND origin == rebinding slot's cell; removal via new hl_cell_unsubscribe_watcher_origin (same loop as the unfiltered variant + origin test; one backref per removed node — symmetry preserved; teardown paths keep the unfiltered variant); re-subscribe preserves origin (constant across moves → each slot's later rebind finds exactly its own nodes = the orphan follow-on resolved by the same mechanism, pinned by the sequential-rebind fixture). hl_cell_subscribe gained the origin param — verified runtime-internal (never in generated C). hl_watcher_new_subscribed passes NULL. HiLowNodeSnap carries origin (inert for notify). The moved-(deep) re-mark now runs only for the rebinding slot's own nodes (attribution applied to deep marking). TESTS: +4 fixtures, outputs predicted before running, all matched, all valgrind 0 — alias_two_watchers (shape 1 pinned), alias_one_watcher (the ruling output: hit hit m1 hit m2 hit m3 done), alias_sequential_rebinds (orphan proof; original container fully drained, keep.push silent), alias_deep (deep deep m1 deep m2 deep m3 done). Integration 314→318; REJECTION_FIXTURES unchanged 26; KNOWN_MEMORY_BUGS EMPTY; ignored stays 2; no fixture deleted, no expected file edited. ZERO-COST vs the β binary (330 entries): 295 byte-identical, 26 no-C-both (= rejection list), 0 one-sided, and the declared diff class held EXACTLY — 9 programs (the 5 pre-existing decl-form container-content fixtures from β + the 4 new alias fixtures), each diffing ONLY in the watcher-construction call (pairs → triples with origins). The plan's declared deviation — the instruction's "pre-existing corpus byte-identical" expectation vs the fact that β's content fixtures are now pre-existing corpus and must diff (attribution requires construction-time slot knowledge; aliased slots deref to the SAME payload cell, so the runtime cannot infer the pairing — that ambiguity IS the bug) — was presented in the plan and adjudicated by approval. SPEC (same commit): one sentence added to the content-following bullet — each followed variable's subscriptions move independently; two variables holding the same value do not move together. Known-issue entry "Aliased-slot retarget edge (3e-β)" REMOVED (resolved). CLAUDE.md phase line stays 4a (not begun). Full ritual green, gate green.

**2026-07-18 Cell migration Phase 3e-β: decl-form content-following — subscription retargeting on rebinding**: Per the approved plan; no STOP conditions hit; Phase 3 complete. SEMANTICS (no new adjudication — the α spec passage decided it): decl-form (changed)/(assigned) on containers are slot subscriptions (rebinding-watch only; the spec's "mutating in place never fires a variable subscription"); the container-subscription nodes of item 10(b) are the content modifiers (added)/(removed)/(moved)/(deep). Consequence pinned deliberately: the old sentinel's shape ((changed)xs + push) is a live program printing nothing (watcher_decl_container_changed_slot_only). RUNTIME: item 10(b) step table implemented as written — HL_SLOT_FOLLOW marker constant; hl_slot_retarget called from both container set functions between store and fire (old alive throughout; release old moved AFTER the fire, step 6); moved (deep) nodes re-mark the new subtree; unconditional under pause/stealth. SOUNDNESS HARDENING adjudicated with the plan (the α analysis's parenthetical claim — notify walk saves next before the body — was REFUTED by the code; the α text itself scheduled β to verify): (A) collect-then-fire node snapshots in hl_cell_notify (direct + per-ancestor loops), active/ended read at fire time; semantic delta — a watcher subscribed during a walk no longer fires for the in-flight event (no fixture pinned the old behavior; verified by survey + zero-diff corpus). (B) hl_notify_depth + deferred-release list: the set family's old-payload release defers while any walk is in flight, drained at depth 0 — the walked cell and bodies' borrowed snapshots of the old value outlive the walk (hazard was reachable in α, unpinned; now closed and pinned by the shape-B fixture + valgrind). DISCLOSED deviation: hl_cell_set_str shares the deferral (same borrowed-snapshot hazard) though it never retargets — the plan said "untouched" in the retargeting sense. CODEGEN: decl-form gate lifts (container content modifiers legal; Added/Removed/Moved/Deep validation now container-only defense-in-depth); construction emits content subscriptions on the payload-deref cell, one FOLLOW node per followed variable, mark_deep at construction for (deep); body prologue dedups snapshot bindings and binds added/removed/moved aliases from the delta (typecheck fills resolved_alias_type form-independently — codegen-only change, as the plan predicted). BOXING: mark_subscription gained the form flag; decl-form container subscriptions (any modifier) set slot_required. TWO LATENT α BUGS fixed (probes hit both; only reachable through slot-boxed containers): (i) `let old = xs` with xs slot-boxed emitted a bare payload borrow tracked as HeapType::Scalar (scope exit called hl_scalar_release on a HiLowArray*) — the Ident-initializer arm now retains and tracks by payload kind; (ii) generate_program_body_functions' pre-pass stored the typechecker's Unknown for inferred container lets → nested functions rejected slot assignment ("type Unknown") — falls back to codegen inference for slot-needing decls only. RECORDED NOT FIXED (below): watcher bodies cannot call nested functions (no forward decls — the soundness fixtures use a companion watcher on a captured trigger instead, same mid-walk shape); aliased-slot retarget edge; NULL-delta mixed-modifier shape. TESTS: sentinel deleted; 7 live fixtures, every output predicted before running, all matched, all valgrind 0; integration 307→314; REJECTION_FIXTURES 27→26; ignored stays 2. ZERO-COST vs the α binary: 294/294 both-sides programs byte-identical, zero diffs (diamond emitted identically this run); 6 new-only; the 7th (assigned_rebind_in_body) is α-legal surface, identical under both. Spec: content-following bullet added; bullet 1 narrowed to (changed)/(assigned). Full ritual green, gate green.

**2026-07-18 Cell migration Phase 3e-α: variable-slot cells — (assigned) + string watching**: Per the approved plan, which ALSO ruled the α/β split (per the instruction's own escape hatch: honest scope exceeded one session; split at the follow-subscription boundary — α = slots + (assigned) everywhere + strings, β = decl-form container content-following with retargeting/deep propagation). STEP ZERO recorded as audit §5 item 10: (a) slot (changed) fires iff unequal under the type's own == (strings by value via hl_string_eq, containers by identity), (assigned) always, changed-before-assigned; (b) expression-form watchers subscribe the VALUE by identity (unchanged — all pinned container content-firing is value-subscription behavior; the fixture survey confirmed NO legacy expectation contradicts (a)), decl-form follows rebinding via slot retargeting (β), (assigned) subscribes the slot in either form. RUNTIME: HiLowValue's STR/ARRAY/OBJECT kinds became slot payloads — adopting constructors, borrowing getters, payload teardown in hl_scalar_release, and the three set functions (adopt +1, compute changed per (a) with an identity fast path before hl_string_eq, store, release old AFTER store — borrowed rhs retained by codegen first so self-assignment is safe, fire through the identical stealth/audience shape as hl_cell_set_i32). BOXING: BoxDecision.slot_required + needs_slot, set by mark_subscription from the modifier + resolved_var_type refcell; the 14 existing boxing tests untouched. CODEGEN: slot lets/assignments/reads mirror the boxed-i32 rows with type-keyed accessors (adoption discipline rides expr_is_borrowed_ref + hl_array_ref/hl_object_ref; compound assignment on ref slots rejected); watcher classification routes slot-kind subscriptions down the scalar/slot body path with type-keyed snapshot bindings; value-kind subscriptions on slot-boxed variables deref the payload to subscribe the CURRENT value's cell. ONE BUG CAUGHT MID-SESSION by the suite: the first deref condition also fired for boxed-i32 (changed) subscriptions (emitted hl_scalar_get_array_ref on an i32 slot — 23 tests segfaulted); fixed to container-types-only, suite fully green after. GATES LIFTED: typecheck (assigned)obj; both codegen string arms; decl-form containers accept (assigned)-only lists (content modifiers still cite 3e-β — pinned substrings preserved, test unedited); slot-needing reference-typed params get a bounded diagnostic (no corpus coverage). DISCLOSED FIX: expression-form (assigned)xs compiled since 3b and silently never fired (subscribed the value cell with HL_SCALAR_ASSIGNED); now fires on rebinding, pinned by watcher_array_assigned_fires which also pins value/slot coexistence (push fires the (added) value watcher; rebinding fires (assigned) only; the value watcher stays with the ORIGINAL container). TESTS: string_watcher_rejected + object_watch_assigned_rejected fixtures deleted (flipped to live surface); +5 live fixtures (watcher_string_changed_fires — equal-value rebinding does NOT fire; watcher_string_assigned_fires — both-modifier order; watcher_string_decl_form_fires — pause/resume on a string slot; watcher_object_assigned_fires — content mutation does NOT fire; watcher_array_assigned_fires) + watcher_mixed_assigned_content_rejected (slot+value in one watcher hits the existing mixed-scalar-container gate) + the old (assigned)obj test rewritten as a compiles-now assertion. Integration 302→307 (plan predicted 306; the delta is that rewrite kept instead of deleted — disclosed). REJECTION_FIXTURES −2+1. Every fixture output predicted from the adjudications before running; all matched; all valgrind 0. SPEC (same commit): the Equality-semantics passage replaced with the subscription-target split (variable/slot vs value), and the (changed)/(assigned) table rows amended. ZERO-COST vs the 3d binary: ALL 287 pre-existing corpus programs byte-identical (modules/diamond verified as the recorded two-variant permutation; 4 new fixtures compile only under α — the flipped surface). KNOWN_MEMORY_BUGS EMPTY; ignored stays 2. Full ritual green, gate green.

**2026-07-18 Cell migration Phase 3d: delete name-keyed subscription**: Per the approved plan. PRE-DELETION INVENTORY (exhaustive reference audit before any edit, every read site verdicted live/dead): watcher_subscribers — one writer (register_watcher_subscriptions), zero value-consuming reads (its consumer died in 3b); heap_watcher_subscribers — ZERO writers since 3b, always empty, its masking restore loop never iterated; the shadow-masking block in generate_function + collect_local_variable_names + two recursive helpers served only those two dead maps; watcher_name_to_id carried exactly two live roles — id transport from the pass-3 allocation loops to the pass-4 consumption loops, and the inference mirror; scalar_watcher_captures / temp_watcher_expr_* / array_watcher_registrations / HeapType::Environment grep-confirmed already gone (3b/2b). SEQUENCED LANDING per plan: (1) id transport replaced ORDER-BASED first, alone — pass 3 pushes allocated ids into a local Vec in item order, pass 4 consumes by position (program-body split: new program_watcher_ids field filled by the generate_program_body_functions pre-pass BEFORE nested functions generate, preserving the id ordering); corpus-wide C diff of the transport-only state: 286 byte-identical, 28 rejection fixtures no-C-either-side, modules/diamond = the recorded module-order permutation (verified: both binaries emit the identical two-variant md5 pair). (2) Then the deletions — both maps, both structs, register_watcher_subscriptions, the masking block, the three collect helpers, generate_watcher's redundant insert — resolving via dead-code checks alone: warning fingerprint delta is exactly 27→25, the two accepted fields-never-read struct warnings from 3b; no new warnings. (3) Inference mirror re-keyed from watcher_name_to_id to variable_types == Type::Watcher (the same key the 3c dispatch arm uses) — the ONE disclosed behavior edge (plan §2a): expression-form watcher method-call inference unified with decl-form (isActive i32→bool; pause/resume/end i32→Nothing on the fall-through). Pinned by new fixture watcher_expression_isactive_print (print(w.isActive()) now true/false; was 1/0, probe-only shape, never pinned). PREDICTION MISS, disclosed: the plan declared zero expected corpus diffs from the re-key; the post-deletion corpus diff found ONE — watcher/expression_methods, where `if (w.isActive())` emits `if (hl_watcher_is_active(w))` instead of `if ((hl_watcher_is_active(w) != 0))` (Bool inference drops the i32 truthiness coercion in two if-conditions). Semantically identical C; runtime output and valgrind verified unchanged; it is the adjudicated §2a unification manifesting, not an undeclared class — but the zero-diff prediction was wrong on that fixture. Everything else corpus-wide byte-identical vs the 3c binary. Phase 3e (variable-slot cells) added to the audit as adjudicated and scheduled immediately next (per the owner's 3d instruction item 4): string watching, decl-form-on-containers, (assigned)obj via boxing extended to reference-typed payloads; the (changed)-reference-equality spec tension resolves there deliberately. Shadowing sentinels (three_level_shadow_probe, same_name_caller_callee, watcher_shadow_array, phase10a capture set) passed unchanged — the audit's 3d gate. Tests 301→302 (+1, no drops); REJECTION_FIXTURES unchanged; KNOWN_MEMORY_BUGS EMPTY; ignored stays 2. Full ritual green, gate green. Module-emission nondeterminism deliberately NOT fixed (plan item 3: recorded, not fixed — the graph walk didn't need touching).

**2026-07-18 Cell migration Phase 3c: runtime watcher lifecycle — statics deleted, both forms share the heap path**: Per the approved plan; no STOP conditions hit. ZERO runtime changes — planning-pass verification showed HiLowWatcher already carried the complete lifecycle (active/ended, hl_watcher_pause/resume/end/is_active, the hl_cell_notify per-node gate) and that the four static helpers were semantically identical to the runtime functions field-for-field, so preservation was a wiring change. DELETED (each named with its replacement): static _active/_ended emission → HiLowWatcher.active/.ended; four static helpers → hl_watcher_*; body static gate → the notify gate (decl-form had been double-gated — the hidden runtime watcher stayed active=true forever while methods flipped only statics); construction-site activation lines + the emit_main_function pre-activation loop → hl_watcher_new constructs active; Phase-5 deactivation loops in BOTH block walkers → the scope-exit hl_watcher_release emitted since 3b (which reaches early returns; the deactivation emission never did); static-dispatch method arm → the existing heap arm, reached by constructing decl-form watchers under the user's own name (Type::Watcher in variable_types + heap_owners under that name). LATENT 3B BUG found by probing and fixed by the renaming: decl-form watcher named `w` segfaulted — hidden var hilow_watcher_{id}_w collided with body function hilow_watcher_{id}_{name} when name=="w"; the uninitialized C local shadowed the function and was passed as body_fn (valgrind: jump to invalid address). Pinned live by watcher_decl_named_w_fires (valgrind 0). MODULE-LEVEL WATCHERS (instruction item 3): probed BROKEN, not inert — parser accepted (incl. export watcher), check_module_bodies never iterated module.watchers, and codegen's bodies-only loop died with "subscription with no resolved type … internal error" on any real one; a module function can't even reference its own export let. Spec specifies no module initialization semantics → wiring a construction site would invent them → REJECTED per the instruction's sanctioned path, on both typecheck paths, diagnostic naming the gap; generate_graph loop deleted (debug_assert guards); fixture modules/watcher_in_module + gate entry; open question added below. ADJUDICATION A (plan approval): decl-form watcher names support method calls ONLY — rejection implemented via a per-Scope decl_watchers set (shadowing-correct: a let rebinding clears the mark; innermost declaring scope decides) with a bare-Ident receiver exemption in the call path; assignment targets covered because check_assign_statement routes targets through check_expression. Previously `return w` emitted non-compiling C and `let v = w` died in codegen — corpus-clean (grep-verified). Spec gained the declaration-form-names-are-not-values paragraph (deliberate, same commit). Decl-form scope-boundness thereby PROVEN; the 3a narrowing recorded in the audit as a future performance refinement, deliberately not implemented. §4.4 ITEM 4 FLIP: watcher_early_return_scalar.hl rewritten from expression-form/function-local (post-exit firing unobservable) to decl-form on a program-scope variable with post-exit mutations after both early-return and normal-exit calls ("fired\ndone") — probed green + valgrind 0 BEFORE the deletion and green after (standing rule; the old shape's expression-form coverage is subsumed by watcher_early_return_array). Sentinel battery (pause/resume, end_is_permanent, isactive_query, expression_methods/pause/coexists/fires/basic/scope, factories, scope-bounding pair, pre-declaration) hand-verified content-identical post-rewiring before the suite. ZERO-COST: 212 unwatched corpus entries — 17 rejection fixtures produce no C under either binary, 194 byte-identical 3b-vs-3c, and the 1 remaining (modules/diamond) permutes run-to-run on the UNMODIFIED 3b binary alone (module emission order: 3/3 md5 split over 6 runs; the 3c binary emits the identical two-variant md5 pair) — a pre-existing module-graph nondeterminism outside the 3a fix's scope, recorded under Known issues below. Warning count went DOWN by one (typecheck's HashSet import is now used); warning fingerprint otherwise identical to HEAD. Two known issues closed (scope-exit deactivation unreachable on early returns; duplicated activation loops). Tests 297→301 (+4), REJECTION_FIXTURES +3, KNOWN_MEMORY_BUGS EMPTY, ignored stays 2. Full ritual green.

**2026-07-17 Cell migration Phase 3b: hl_cell_set — boxed scalars lower to cells; escape rejection dropped**: Per the approved plan; ONE STOP condition hit and adjudicated mid-session (fire order, below). RUNTIME: HiLowScalar {HiLowCell cell; HiLowValue value} — HiLowValue IS the one payload representation from the 3a design statement; only hl_scalar_new_i32/get/retain/release + hl_cell_set_i32 landed (corpus kinds = {i32}; §5 item 7 allow-list unchanged — no new kind, nothing lifts, no speculative matrix). hl_cell_set_i32 stores always (stealth suppresses only notification), fires CHANGED iff the payload differed then HL_SCALAR_ASSIGNED (new constant 6) always — changed subscribers before assigned, the legacy order (probed pre-change, preserved byte-for-byte on the sentinel outputs). hl_cell_notify amended: CHANGED/DEEP modifiers do not fire on HL_SCALAR_ASSIGNED and it never triggers the deep parent walk (an equal-value assignment is not a mutation). HiLowWatcher gained env_dtor; hl_watcher_new_subscribed gained the dtor param; EVERY env slot (subscribed + captured, containers included — adjudication F) is a RETAINED cell released by a generated per-watcher dtor; the runtime frees the env. LOWERING: boxed let → hl_scalar_new_i32 + HeapType::Scalar scope/early-return release; reads → hl_scalar_get_i32; assignment INCLUDING COMPOUND (adjudication B — the Phase 10-γ rejection and the expr-form silent no-fire, both probed, died) → hl_cell_set_i32; `return x` copies the payload and does NOT ownership-transfer the cell (a leak found by the pre_declaration fixture and fixed); boxed params box in the prologue (name rebinds); boxed PROGRAM-scope lets become file-scope statics — the factory fixtures subscribe program vars from nested functions, which legacy served via a name-keyed accident (fire sites in main referencing a coincidentally same-named binding; see the generated-C archaeology in the session transcript) and cell identity now serves soundly. WATCHER PATHS UNIFIED: expression-form scalars moved off the 2b heap path onto the 2c env-ABI cell path; decl-form bodies are env-ABI too, gated on the legacy statics (stay until 3c), constructed at the DECLARATION SITE as a hidden scope-owned watcher — pre-declaration assignments can't fire because the cell has no subscriber yet (replacing the compile-time active-flag trick; pinned fixture unchanged). Capture lists for scalar/decl-form watchers come from the 3a analysis (new BoxingAnalysis::captures_for — shadow-correct, per-watcher, covering every crossed boundary); container watchers keep the legacy list (its phantom subscribed-container slots are the multi-subscription rebind mechanism) with unboxed-scalar phantoms skipped. Adjudication C fell out free: decl-form bodies referencing outer variables WORK (previously emitted C that failed to compile — probed). DELETED: scalar firing block + compound rejection (codegen ~2249–2398), the 2b scalar WatcherExpr branch (bare hl_watcher_new + temp_watcher_expr_* side-channel + _cap_ pointer params + scalar_watcher_captures read branch), emit_watcher_call_args{,_from_names}, extract_watcher_id, and the typecheck escape machinery (capture_unsafe_watchers, current_function_scope_depth, check_watcher_escape_reachability, both rejection sites) with the spec's reachability rule replaced by sound-escape text (Low-mode prohibition kept). STOP HIT — FIRE ORDER (ruled mid-session, recorded as audit §5 item 9): one firing path exposed that the scalar suite pins declaration order (nested_watchers, expression_coexists) while three 2c-era array fixtures pin REVERSE order (hl_cell_subscribe's prepend artifact) — contradictory for the same abstract program, no discipline satisfies both (verified both directions). Owner ruled DECLARATION ORDER: subscribe appends; the three array expected files (added_and_changed_both_fire, moved_changed_both_fire, array_insert_watcher_fires) flipped deliberately as part of the ruling; spec gained the fire-order sentence. TESTS: −3 escape rejections (incl. test_watcher_capture_escape_rejected — the survey found THREE, not the two the phase line named; fixtures deleted, gate −3), +7 new (watcher_changed_assigned_order, watcher_compound_assign_fires, watcher_decl_capture_fires, watcher_escape_capture_sound — the previously-rejected direct-return shape, fires after escape, valgrind 0 —, watcher_escape_subscribed_local_sound — the reachability case, now inert not unsound —, watcher_decl_container_rejected (adjudication A: was untested, LEAKING, rebinding-firing; now a clean diagnostic; same variable-slot bucket as (assigned)obj and string watching), watcher_destructured_binding_rejected (adjudication E)); gate +2 rejection entries. String watching stays rejected; the message now names the variable-slot bucket while preserving both pinned substrings (no test edit). ZERO-COST: 211 unwatched compilable corpus programs byte-identical HEAD-vs-3b (the 14 remaining are module-entry/rejection fixtures that never reach codegen; an earlier diff run raced the concurrently-running test suite's temp dirs and was discarded — never run the compiler concurrently with the gate). 2b laundering residual closed as resolved-by-design. Full ritual green: integration 297/0/2, gate green, KNOWN_MEMORY_BUGS EMPTY.

**2026-07-17 Cell migration Phase 3a: boxing analysis (+ step-zero ruling record, deterministic cleanup emission)**: Per the approved plan; no STOP conditions hit. STEP ZERO (user ruling, recorded as audit §5 adjudicated item 8): dynamic property addition's design direction is OPEN-SHAPE OBJECT TYPES — declared open tail; dynamic reads on an open object type as T? yielding unknown-with-reason when absent; (added) legal only on open objects; phase UNSCHEDULED, post-migration alongside the string-literal revision; the 2e rejections stand until then (STATUS open question updated in place — no longer awaiting a design decision, only a phase slot). ANALYSIS: new `src/typecheck/boxing.rs` (pub mod under typecheck). Invariant: declaration D boxes iff (a) a subscription — decl-form or expression-form — resolves to D, or (b) a watcher body references D (read OR write — assignment targets count) across the watcher boundary. Conservative-correct: uncertain ⇒ box. Both watcher forms' captures box — expression-form values are first-class (incl. the 2b laundering hole), and decl-form NAMES are also first-class Watcher-typed variables today, so scope-boundness is not provable for either; 3c may narrow decl-form if its runtime lifecycle pins it. Function-expression closures are not watchers and don't box captures, EXCEPT when nested inside a watcher body (the boundary check still fires — conservative). Type-agnostic: marks declarations; container marks are subsumed (they're already cells); 3b applies the attribute to scalars only. Implementation: standalone AST walker with its own scope stack (keyed lookups only) and watcher-boundary indices; subscriptions resolve OUTSIDE the watcher scope; subscription bindings/aliases/params/body-lets are declarations that shadow correctly. Queryable API: is_boxed(name, decl_pos), decisions_for(name) in declaration order (shadowing tests assert by index, no hardcoded positions), boxed_count; NOT wired into the compile pipeline (nothing consumes it until 3b — the pub API is the attribute). THE 2E FINDING, examined per instruction: 3a provably does NOT inherit it — the analysis never reads WatcherExpr.captures, re-deriving references shadow-correctly; pinned by test_body_local_shadow_does_not_capture_outer (the exact case the legacy scan gets wrong) plus two more shadowing pins; NOT fixed here — the legacy scan feeds live env-packing codegen (behavior change, out of gate) and dies with its machinery in 3b–3d. REPRESENTATION (design statement only, recorded in the audit annotation): boxed scalar = HiLowCell header + {kind, payload union}, cell first member; ONE payload representation serves both boxed scalars and the §5 item 7 optional payload matrix — HL_OPT_* kinds converge onto it; the 2b allow-list rejection lifts as kinds land, not before; nothing added to runtime.h this phase. TESTS: new tests/boxing_analysis_tests.rs, 14 tests, every one asserting concrete decisions (unboxed plain + closure-capture; subscription both forms; capture read + capture write; the laundering escape shape — which COMPILES today per the documented 2b residual hole — boxes the local; three shadowing pins; container subsumption; alias non-boxing; nested-watcher boundary crossing). All expectations derived from the invariant before running; all 14 passed on the first run. DETERMINISM RIDER: FOUR emitting HashMap-iteration sites, not the planned three — emit_scope_cleanup + emit_early_return_cleanup (heap_owners), emit_temp_cleanup (temp_owners), and emit_enclosing_temp_releases (per-frame saved temp_owners; found during implementation) — all collect-sort-by-name-emit now. Generated C byte-identical across 5 regenerations each on 4 multi-owner programs (incl. array_insert_watcher_fires, the 2e permuter); Known-issues entry closed. Full ritual green: integration 293/0/2 unchanged, boxing_analysis 14/0 new, gate green (valgrind re-proved the release-order change clean across all ~250 programs), KNOWN_MEMORY_BUGS EMPTY, REJECTION_FIXTURES unchanged.

**2026-07-17 Cell migration Phase 2e: objects join the cell model**: User ruling opened the session: objects gain the cell header NOW, as 2e before boxing (containers complete before scalars box) — resolves the open question 2d surfaced; audit phase plan and CLAUDE.md updated accordingly (3a follows 2e). Per the approved plan, with six plan-adjudicated items (A–F below). HEADER: `HiLowObject.refcount` → embedded `HiLowCell` first member (weak_refs list survives); hl_object_retain/release are cell-based with the 4-step typed teardown on zero (weak unregister → strong release with backref unlink first → weak-slot nulling → free); hl_object_ref updated; no other direct refcount uses existed. CONTAINMENT: all object stores funnel through set_property (strong) and hl_object_set_object_weak (weak) — object_property_stored/removed mirror 2d's array helpers (strong OBJECT/ARRAY values only; add after retain + mark-deep into marked holders; remove BEFORE release on overwrite and teardown). A weak store adds NO link but still FIRES on the holder (weakness affects containment, not firing). Array-side: 2d's mutators already called the helpers for every element kind — they just early-returned unless elems_are_arrays; added elems_are_objects (retain_fn == hl_object_retain) and the object branches. Deep marking is now mutually recursive (hl_object_mark_deep skips weak properties; invariant is marked ⇒ STRONG-reachable subtree marked). EVENTS: set_property computes `existed` before the store and fires `existed ? CHANGED : ADDED` with NULL delta under the 2d guard shape; proto reassignment is an existing-property set → CHANGED; no REMOVED path exists to instrument. hl_cell_notify: ZERO changes. ADJUDICATED (A): dynamic property addition is NOT expressible (`o.y = 2` on `{x:1}` → "Cannot assign i32 to nothing"), so runtime ADDED has no reachable trigger — mapping implemented in full, but `(added)obj` REJECTED with a diagnostic (a subscription that provably can't fire is a trap); new Open question to schedule dynamic property addition. (B): true object cycles unrepresentable (self-property and proto-cycle probes both type-rejected) — object diamond fixture pins single-fire via the same epoch suppression. (C): array-valued object properties did not exist (no HL_VALUE_ARRAY; codegen UnsupportedFeature) — landed minimal: value tag, hl_object_set_array / hl_object_get_array (borrow, proto-chain walk), ownership arms in set_property/release, TYPE_ARRAY for for-in totality, codegen arms (member assign, literal property, member read, let-binding retain via existing patterns). (D): `(assigned)obj` rejected citing Phase 3. (E): string properties create no links (string watching still rejected, §5 item 2). (F): decl-form object watchers stay rejected (legacy name-keyed path, dies in Phase 3) — objects are expression-form cell surface only. CODEGEN SURFACE: WatcherExpr admits Object; classification is cells-vs-scalars; mixed array+object rejected (prologue casts the fired cell to the FIRST subscription's container type); body prologue emits `HiLowObject* o = (HiLowObject*)hilow_cell;`; deep hoist emits hl_object_mark_deep for object subs. SURPRISE #1: subscribed names are ALSO collected as watcher captures (find_variable_in_outer_scope ignores the subscription's shadowing declaration) — arrays never exposed this because array captures are identity-stored pointers; the object fix joins objects to the identity-capture branch (env struct field, packing, and the Ident-emission deref/paren-close all keyed on container type now). SURPRISE #2, found by the zero-cost check: generated C is NONDETERMINISTIC across runs of the SAME binary — scope-cleanup release order comes from HashMap iteration (verified on the unmodified 2d binary: run-to-run diffs permute hl_*_release lines identically to the pre/post diff) — recorded under Active deferred work; prior phases' "byte-identical" checks were luckier than they knew. Zero-cost result: watcher-free object program byte-identical pre/post-2e; non-deep array program identical as a line multiset (only the pre-existing release-order permutation); watcher_reentrant_deferred live stdout byte-identical pre/post (HEAD worktree, no stash). VERIFICATION: 16 scratchpad probes, every fixture output predicted from semantics before running and every observation matched, valgrind 0 across all; 18-sentinel re-run (2d deep set + 2c set + 1.5c object set) all exact-output + valgrind 0 (two apparent diffs were expected-file trailing-newline artifacts; the third — reentrant vs its Phase 5 expected file — is the documented ignored-test gap, live output unchanged). Twelve live fixtures + four rejection fixtures landed. Integration 277→293, ignored stays 2, KNOWN_MEMORY_BUGS stays EMPTY, REJECTION_FIXTURES +4. Spec gained the weak/no-deep line (instructed, same commit).

**2026-07-17 Cell migration Phase 2d: parent lists + deep propagation (arrays only)**: Per the approved plan, with both plan-presented STOP-items adjudicated by plan approval. PARENT LINKS: `HiLowCellParent` entries live on the CHILD, non-owning, one per containment — duplicate containments carry duplicate entries and each removal (hl_cell_remove_parent) drops exactly one; the walk's epoch stamp, not entry uniqueness, prevents double-fire (pinned by the diamond fixture). Maintained at every containment change: push/insert/set add (via `array_element_stored`), pop/remove/set-overwrite add nothing—remove one (via `array_element_removed`), clear and parent-teardown remove one per element BEFORE releasing; move is reorder-only. Element kind detected by retain_fn identity (`retain_fn == hl_array_retain`); child death while contained is impossible (parent holds a refcount) so no child-side unlink path exists. WALK: `hl_cell_notify` gained deep semantics — own-list nodes also fire on HL_ARR_DEEP (a deep subscriber on the mutated array fires for EVERY mutation, per spec), then, only when `deep_watched && parents`, collect-then-fire: ancestors gathered under a fresh `hl_deep_epoch` (version-field stamping; diamonds/future cycles collapse to one visit), then ONLY their deep nodes fire, with the SAME delta (one per mutation, NULL for set/clear) and their own cell (body binds the subscribed variable's current value, per the brief). Collect-then-fire keeps traversal atomic under re-entrant mutation in deep bodies. STOP-ITEM (B, adjudicated): a truly self-containing array is UNREPRESENTABLE (no recursive types — `xs.push(xs)` is an element-type mismatch), so the instructed self-containment cycle test cannot be a .hl program; the diamond fixture (same child twice in one parent) exercises the identical revisit-suppression mechanism, which also terminates true cycles if ever expressible. DEEP-BIT: set by `hl_array_mark_deep` (codegen hoists it per (deep) subscription via pending_statement_decls; recurses the subtree, early-returns on a set bit under the marked⇒subtree-marked invariant) and at containment-add into a marked parent (pinned by the new-child fixture); CLEARING DEFERRED (Known issues) — stale bit = one wasted silent walk, never a wrong fire (pinned by the unsubscribe fixture). Stealth suppresses deep via notify's single gate (pinned). SURFACE (audit §5 item 3): `deep` parser word + `SubscriptionModifier::Deep` + typecheck arm (DynamicArray only; diagnostic cites Phase 3 boxing for scalars and names objects UNSCHEDULED); alias-on-deep rejected by the existing added/removed/moved-only alias rule (precise deep deltas deferred, per the brief's "optional alias data"); decl-form generate_watcher rejects Deep alongside Added/Removed/Moved; HL_ARR_DEEP=4 fills the documented constant gap. STOP-ITEM (A, adjudicated): the audit is SILENT on which phase objects embed the cell header — deep crosses array-in-array ONLY, object-held arrays do not propagate; recorded as a new Open question (user scheduling decision). Zero-cost: generated C for a non-deep array program byte-identical pre/post-2d (verified via a HEAD worktree build + diff, NOT stash — a stash round-trip attempt tripped on the repo's pre-existing stash stack and was abandoned; the old stash entries are untouched); unwatched mutation cost = the `cell_has_audience` bit+pointer check. Seven live deep fixtures (direct, 3-level nested, sibling isolation, diamond, unsubscribe, stealth, new-child) all probed empirically — outputs matched the semantics-derived predictions exactly, valgrind 0 — plus one rejection fixture; 13-sentinel 2c set re-verified (exact output + valgrind 0). Integration 269→277, ignored stays 2, KNOWN_MEMORY_BUGS stays EMPTY.

**2026-07-17 Cell migration Phase 2c: one firing ABI + value deltas**: Per the approved plan, no brief/audit conflict (audit 2c's "un-ignore the re-entrancy test" was already superseded by the 1.5d ruling — noted in the audit annotation). RUNTIME: new `HiLowDelta {event, payload, payload_size, payload_release, from, to}` — heap-owned, self-contained, QUEUEABLE (owns a copy of the element bytes; retains object-array element refs via retain_fn at construction, released by hl_delta_release; no pointers into array storage, caller stacks, or statics; delta/scratch allocations deliberately uncounted by hl_alloc/free_count — always balanced inside one mutator call, valgrind-visible). Constructors hl_delta_new_elem / hl_delta_new_moved (moved carries from,to inline; CHANGED-only notifications pass NULL). `hl_cell_notify(cell, event, delta)` is the ONE firing path: no-ops under stealth (its authoritative site), then ONE walk of cell->watchers in list order firing active/un-ended nodes where modifier == event OR modifier == CHANGED — the implicit-changed single walk mirrors the old interleaved per-mutator loops exactly, preserving the 1.5d-pinned firing orders (verified: array_insert_watcher_fires byte-exact). All eight mutators became guard (stealth+non-empty list, to skip delta construction only) + construct + notify + release; no mutator walks the list. One body ABI `HiLowWatcherBody(void* env, HiLowCell* cell, const HiLowDelta* delta)` for every modifier — §3.4(a)'s 2-arg env-dropping casts are dead at BOTH .move sites (real and from==to). Both static temp_buffers deleted (grep-verified they were the only two statics that weren't functions): hl_array_remove signature is now `(arr, index, void* out)` — caller-owned destination, no 1024-byte cap, re-entrant, and the REMOVED delta is copied before the shift; hl_array_move's shift scratch is a local malloc/free. CODEGEN: array watcher bodies emit the new signature, rebind the watched name via `HiLowArray* xs = (HiLowArray*)hilow_cell;` (preserving pre-2c semantics incl. the multi-array first-name-binds-whichever-fired quirk), bind added/removed aliases from `delta->payload` and moved aliases as `HiLowMovedDelta m = {._0 = delta->from, ._1 = delta->to}` (HiLowMovedDelta survives only as the Tuple(Usize,Usize) binding type); `.remove(i)` emits `(hl_array_remove(arr, i, &tmp), tmp)` with tmp hoisted via pending_statement_decls. CHANGED watchers now receive the primary delta instead of NULL — unobservable, since (changed) subscriptions have no alias binding. Behavior preservation checks: watcher_reentrant_deferred stdout byte-identical pre/post (stash round-trip; still synchronous ordering, aliases still copied at body entry) and valgrind 0 — its test correctly stays ignored for Phase 5; 20 sentinel programs exact-output + valgrind 0, including watcher_move_capture_env now CLEAN (was 2 errors) → test LIVE, KNOWN_MEMORY_BUGS EMPTY. New fixture watcher_move_noop_capture_env (+expected +live test) pins the from==to firing site with captures, which no 1.5d fixture covered. Known-issue entry for the hl_array_remove static buffer removed (resolved). Integration 267→269, ignored 3→2.

**2026-07-16 Cell migration Phase 2b: watcher-owned envs + scope-owned-env machinery deleted (step zero: optional-inner rejection)**: Two-part phase per the approved plan. STEP ZERO (audit §5 item 7, adjudicated this session): optional inners without runtime payload kinds are rejected at compile time citing Phase 3 — new recursive `validate_declared_type` (allow-list i32/string/time/duration/money/MoneyOf + the internal Object case weak reads produce) wired into check_function, check_function_expression, check_let_statement annotations, and TypeAscription; walks nested positions (array/tuple elements, function-type params/return, T??). The three enabling bugs from the 2a requirement-0 audit each reject cleanly or are fixed: (1) new `current_function_return_type` on TypeChecker drives a NARROW return-type check — returns into a `T?`-declared function must be T, T?, or unknown ("cannot return string from a function declared to return i32?"); the general return-type gap deliberately stays an open question; (2) the return-wrap `_ => hl_optional_new_i32` catch-all became explicit arms per payload kind with a hard-error default — and gained time/duration/money arms plus matching wrap-predicate entries, so `time?`-returning user functions (previously a C compile error) now work; (3) `emit_refined_variable_access` became Result with a money arm; its raw-variable fallback (emitted HiLowOptional* where the narrowed C type was expected) is now a hard error. print(T?) acceptance trimmed to {i32, string} (the constructible+printable intersection). PHASE 2B CORE: `HiLowWatcher` gains `void* env`, stored by `hl_watcher_new_subscribed`, freed (with hl_free_count++) on final release after unsubscription — the watcher's refcount IS the env's refcount, a deliberate interpretation of "refcounted envs" while envs are unshared (stated in the audit annotation). Deleted: `hl_cell_unsubscribe_env` (runtime), `array_watcher_registrations` (all 5 sites), the `HeapType::Environment` variant and its three cleanup arms (scope/early-return/temp), and the WatcherExpr arm's env scope/temp tracking. STOP-ITEM (resolved via plan approval): capture-escape was REACHABLE — the escape check ran only on direct WatcherExpr returns and never inspected captures, so `let w = watcher((added)param_arr){ print(local) }; return w` passed and was kept sound only by the deleted net (envs store scalar captures as &stack_local). Now rejected at compile time: check_watcher_escape_reachability walks captures (moved after check_expression so captures are populated), and check_let_statement records capture-unsafe watcher bindings (per-function map, cleared on rebinding) that check_return_statement rejects — "watcher 'w' captures 'z' … cannot escape its declaring function until Phase 3 (scalar boxing)". Subscriptions need no new check on the `return w` path: 2a's symmetric unlink makes dead-array subscriptions sound (escaped watcher goes inert), and the pinned no-capture factory stays legal. RESIDUAL known hole (documented below): laundering a capture-unsafe watcher through a helper call is not caught — needs dataflow analysis that Phase 3's boxing (which drops the escape restriction entirely, §5 item 1) obsoletes; such shapes were already silently inert before this phase. Audit-2b's "write §4.4 items 2 and 3" verified already satisfied by 1.5d (item 3 flipped in 2a, item 2 flips in 2c — the audit's own footnote). Targeted verification before the suite: 13 sentinel programs (optional users, §3.4(b)(c)(d), factory, early-return, dies-with-scope, all four capture tests, by-reference) exact output + valgrind 0; watcher_move_capture_env still exactly 2 valgrind errors. Four rejection fixtures with is_err tests on REJECTION_FIXTURES. Integration 263→267, ignored stays 3; KNOWN_MEMORY_BUGS unchanged = [watcher_move_capture_env.hl].

**2026-07-16 Cell migration Phase 2a: cell header onto HiLowArray + registration by construction**: Landed the brief's cell header as a standalone `HiLowCell {refcount, watchers (HiLowCellWatcher list), parents (dead until 2d), version (dead), deep_watched (dead)}` embedded as HiLowArray's FIRST member; `HiLowArray.refcount`/`.watchers` and the old `HiLowArrayWatcher` struct deleted (migrated, not duplicated). Cell operations all take HiLowCell*, nothing array-specific: hl_cell_retain; hl_cell_release (returns true on zero after tearing down subscription list — unlinking each node's watcher backref — and parent list; the typed container then does its own teardown+free); hl_cell_subscribe; hl_cell_unsubscribe_watcher (all nodes for a watcher, symmetric backref unlink); hl_cell_unsubscribe_env (legacy env-keyed first-match removal — the scope-owned-env safety net, dies in 2b). hl_cell_notify is deferred to 2c and the parent walk to 2d per the audit. SCOPE ADJUDICATION (via the approved plan, requirement 4): the phase instruction's "watcher construction must register by construction / §3.4(d) structurally impossible" cannot be layout-only — structural registration forces release-unsubscribes, which needs watcher→cell subscription backrefs (HiLowWatcher gained `subs`; cell→watcher and watcher→cell links both non-owning, whichever dies first unlinks itself from the other). So §3.4(b)(c)(d) are FIXED here rather than 2b: `hl_watcher_new_subscribed(body_fn, env, n, (cell,modifier)...)` is now the ONLY attach path, emitted as the watcher expression's value in ANY syntactic position (the `temp_watcher_expr_*` side-channel — the (d) hole — is deleted for arrays, kept for scalars until Phase 3); temp-position watchers hoist as tracked temps whose statement-end release unsubscribes; scope/early-return watcher releases now really unsubscribe. Their three tests are LIVE (all "after", exit 0, valgrind-clean) and their KNOWN_MEMORY_BUGS entries removed; the env-keyed safety net (array_watcher_registrations + Environment cleanup arms, now emitting hl_cell_unsubscribe_env) is retained solely for the capture-escape case until 2b's owned envs, single-slot limitation unchanged. §3.4(a) DELIBERATELY PRESERVED: all eight firing loops keep byte-identical call shapes including the 2-arg .move casts; watcher_move_capture_env verified still 2 valgrind errors (gate honesty). Live sentinels hand-verified before the suite: array_watcher_factory (transferred watcher keeps firing), watcher_early_return_array, watcher_shadow_array, array_insert_watcher_fires, array_watcher_dies_with_scope, watcher_early_return_scalar — all exact output + valgrind 0. Requirement 0: the six placeholder optional-unwrap helpers (i64/u32/u64/f32/f64/bool, formerly `return 0;`) proved REACHABLE from valid programs and were converted to loud aborts (fprintf + abort); the enabling bugs are recorded as open questions below. Integration 260→263 live, ignored 6→3; KNOWN_MEMORY_BUGS = exactly [watcher_move_capture_env.hl].

**2026-07-15 Cell migration Phase 1.5e: weak-after-death semantics (audit §5 item 6)**: Implemented the adjudicated behavior — dead-weak read yields `unknown` with reason "weak referent released"; member access propagates per the spec's unknown rules — by typing weak property reads as `T?` and riding the EXISTING Phase 9 unknown ecosystem (Type::Optional is literally documented "T or unknown": hl_is_unknown runtime is-check, is-unknown refinement to UnknownType driving `.reason` via hl_optional_unwrap_unknown, print_optional_* dispatch) instead of building a parallel weak-unknown mechanism. Design: a weak slot's type in the object shape is `Optional(referent)` (set at the literal/typecheck level; WeakRef expressions themselves keep their inner type so store sites are untouched); reading it emits new runtime `hl_object_get_weak` returning a fresh +1 optional — retained referent while alive (reading a weak upgrades to strong for the binding's lifetime), unknown "weak referent released" after death (referent death nulls the slot via the 1.5c (holder,prop_index) bookkeeping, so found-property-with-NULL IS the dead state); member access through the optional emits `hl_optional_member_{i32,str,object}` — an unknown propagates as the SAME HiLowUnknown instance (retained), a live referent's property wraps as T? (nested weak slots flatten: unknown does not nest); HiLowOptional gained HL_OPT_OBJECT + owned object payload (released in hl_optional_release). Codegen: optional-returning member reads hoist as HeapType::Optional temps at statement level (mirroring the .reason temp pattern); let-bindings of them track as Optional owners. Surface changes beyond the literal ruling, both forced by it and reported in the audit annotation: `print(T?)` now typechecks for primitive/string inners (codegen's print_optional_* dispatch already existed but was reachable only under refinement — the pinned alive-path `print(holder.ref.name)`, now string?, needed it; known state prints the bare value, so weak_basic/weak_breaks_cycle outputs are unchanged) and assignment-form weak stores (`holder.ref = weak target`) accept the T?-slot vs T-value pairing. LIMITATION (judgment call, flagged for review): member propagation through a weak read is implemented for property types i32/string/object only — other property types (i64, f64, bool, function, money…) through a weak read raise a compile-time UnsupportedFeature diagnostic, because the optional runtime lacks those payload kinds (its unwrap helpers for them are pre-existing placeholders returning 0); extending the matrix is deferred rather than silently miscompiled. Spec edits in the same commit: new "Weak References" subsection under High-mode memory (no retain on store, slot nulled on referent death, dead read yields unknown with the stated reason, T? reads, propagation, code example) replacing the lone cycle-detection mention, and the line-564 flexible-objects claim amended: "added or removed dynamically" → added dynamically; removal not implemented; property indices are append-only; future removal tombstones slots, never compacts or reuses indices (WeakRef keying and slot-stability depend on it). test_weak_after_death_unknown_integration un-ignored and LIVE (its 1.5d expected file passes byte-exact); weak_after_death_unknown.hl removed from REJECTION_FIXTURES — it now compiles, runs exit-0, and is valgrind-clean under the gate, as are weak_basic and weak_breaks_cycle with the new codegen. Integration 259→260 live, ignored 7→6.

**2026-07-15 Cell migration Phase 1.5d: gap tests (tests only — zero compiler/runtime changes)**: Wrote every audit §4.4 pre-migration gap test, each behavior probed empirically (compile + run + valgrind in scratchpad) before being committed as a program, and classified for the valgrind gate (which runs every .hl under tests/programs/). LIVE pins, all valgrind-clean: watcher_early_return_scalar (item 4 — early return causes no spurious fire; non-early path fires once), watcher_early_return_array (item 4 — 1.5b early-return cleanup unregisters, caller's later push fires nothing), watcher_shadow_array (item 5 — array subscription is by identity; a shadowing xs in a nested function doesn't fire the outer watcher), array_insert_watcher_fires (item 6 — insert fires CHANGED then ADDED, alias carries the inserted value; firing ORDER pinned as observed), array_watcher_factory (item 7 — no-capture watcher on a caller-owned array returned from its declaring function keeps firing), test_stealth_return_rejected (item 9 — fixture existed since before 1.5b with no test; now asserts the "return inside stealth block" message), test_string_watcher_rejected (item 8 per §5 item 2 — pins the actual current diagnostic "watching value of type Primitive(String)… string and composite watching"; the adjudication's aspirational wording would need a compiler change, out of scope here). SIX #[ignore]d expected-behavior tests (all reasons in the ignore strings and in Known issues below): re-entrant mutation (item 1, per user ruling asserts deferred declaring-thread-queue semantics — expected file records the deferred ordering AND delta-alias survival across a nested remove; current synchronous nested firing differs; flips Phase 5); §3.4(a) move+captures (segfaults today — 2 valgrind errors), §3.4(b) multi-array capture unregister (segfaults — 3 errors), §3.4(c) "NULL"-keyed no-capture registrations (both watchers fire after scope death + 4 invalid reads — NOT valgrind-benign, contra the audit's "benign today" note), §3.4(d) statement-temporary watcher expression — probing found the current failure is UPSTREAM of the audit's free-without-unregister description: a watcher expr passed as a call argument never registers its subscriptions at all (temp_watcher_expr_* side-channel is consumed only by the let path) and leaks (exit 1); all four programs added to KNOWN_MEMORY_BUGS with citations (first re-population since 1.5c emptied it — sanctioned by the §5 item 4 adjudication). Weak-after-death ADJUDICATED (recorded as audit §5 item 6): dead-weak read yields unknown with reason "weak referent released", member access propagates per the spec's unknown rules; implementation is NEW Phase 1.5e with the spec edit in the same commit; expected-behavior test written now, #[ignore]d citing 1.5e, program on REJECTION_FIXTURES (`.reason` on an object-typed value doesn't compile today — the honesty check will force the entry's removal when 1.5e lands). Object property removal verified NONEXISTENT (no parser syntax, no hl_object_remove_* in runtime; spec line 564's "removed dynamically" is aspirational) — so no weak-across-removal test is writable; the constraint is recorded next to the WeakRef (holder, prop_index) re-keying note in the audit, which that keying's append-only assumption depends on. Probe surprises: `watcher((added)xs){…}.pause()` doesn't parse (statement-position watcher is declaration form) and `.isActive()` on a watcher expr fails codegen — the call-argument form is the only reachable statement-temporary watcher. Integration 252→259 live (+7), ignored 1→7.

**2026-07-15 Cell migration Phase 1.5c: object ownership discipline**: Fixed the 17-program object double-release class by deriving every fix from one axiom (every store of a heap reference retains; every overwrite/removal/owner-death releases exactly once; weak references neither retain nor release) rather than per-program patches. Plan-mode inventory (23 store/drop sites classified retain-missing/release-missing/correct) found the root asymmetry: `set_property` adopted on NEW keys (no retain) but `hl_object_release` strong-releases every non-weak property — so any borrowed local stored as a property/proto sat on two release paths with one refcount. Runtime: set_property now retains on both paths (and its overwrite path unregisters weak old values instead of releasing them); hl_object_set_str's pre-retain removed (it double-retained on overwrite — a latent leak); new hl_object_set_object_weak (is_weak=true, no retain, registers weak ref — `is_weak` was NEVER set anywhere before this session, so weak was entirely unimplemented: literal-form weak was a plain strong unretained store, assign-form registered the location but still strong-released); WeakRef re-keyed from a raw slot address to (holder, prop_index) — the old interior pointer dangled when ensure_capacity realloc'd the properties array; new hl_object_ref/hl_function_ref/hl_array_ref retain-and-return helpers. Codegen: two central classification fns (expr_is_borrowed_ref; needs_site_release_after_store) drive all store sites — object-literal properties, member assignment (value now generated in Temporary context so tracked temps balance the store's retain; untracked fresh values — object/function literals, f-strings — get site releases), array literal/push/insert/index-assign releases now conditional on freshness (they previously released borrowed locals they never owned, cancelling the array's retain); Object/Function member reads are now pure BORROWS (1.5a had them registered as tracked temps around borrowing getters — statement-end then released references the property still owned; this alone was weak_basic's `holder.ref.name` failure); let-bindings of member/element reads retain+track (strings track only — hl_object_get_str returns +1); reassignment from a borrowed RHS retains BEFORE releasing the old value (x = x safe); for-in array element bindings retain per iteration (release at iteration end + LoopFrame extra_cleanups + heap_owners, mirroring the 1.5b key pattern); discarded pop/remove releases the moved-out element (extended to nested-array elements); returns of borrowed object/function values wrap in hl_object_ref (return is always +1 to the caller); object/function-typed match arms normalize to +1; tuple literals retain borrowed heap elements. Closure env captures audited: object captures are borrows on both sides (env_dtor doesn't release them) — consistent, no change; the dangle-if-escaping case is the §3.4 env-keying latent-bug territory (1.5d tests). KNOWN_MEMORY_BUGS emptied; the gate passes with all ~230 programs clean, zero regressions from the retain-on-store change on the first full run. Six pinning tests added (object_two_arrays, proto_reassign, array_element_overwrite, object_shared_two_fields, object_field_overwrite, object_string_prop_overwrite; `shared` turned out to be a reserved word — renamed the test variable). Weak-after-death behavior verified and reported (see Open questions). Integration 246→252.

**2026-07-15 Cell migration Phase 1.5b: control-transfer temp cleanup + valgrind gate**: (1) Fixed the two 1.5a-disclosed leaks with pinning tests written FIRST and shown failing (exit 1, MEMORY LEAK) before the fix: `generate_statement` now stacks enclosing statements' temp frames (`enclosing_temp_frames`) instead of a local save, so `return` releases every enclosing statement's temps (walking all frames), and `break`/`continue` release frames above the innermost `LoopFrame`'s base plus the loop's extra cleanups (the object for-in key). LoopFrame/temp-frame/switch-flag state is per-C-function (take/restore at both function-generation boundaries — an initial version missed taking `temp_owners` there and regressed method_this_basic/proto; caught by the per-fix suite run). A `+1` off the frame base excludes the loop statement's own temps from break-path release (its statement-end cleanup still runs). Break semantics disambiguated: string-switch break is a full no-op (the old `emit_early_return_cleanup` there was a mid-flow double-release — found by the gate on switch_string, fixed), C-switch break emits bare `break;`, loop break does frame + scope cleanup; loops clear both switch flags around bodies. New tests: return_in_match_arm_temps, return_in_switch_case_temps, for_in_key_break, for_in_key_continue. (2) Added tests/valgrind_gate.rs: discovers every .hl under tests/programs/ (app.hl = entry when present), compiles (explicit REJECTION_FIXTURES skiplist with per-entry test citations; a fixture that unexpectedly compiles fails the gate), runs valgrind (--leak-check=full, definite+indirect as errors), parses ERROR SUMMARY (program exit codes deliberately ignored — two fixtures exit nonzero by design), 8-way threaded, ~24s. First run: 223 clean, 24 failing — STOPPED and reported per instruction; all failures verified pre-existing by stash/rebuild/valgrind at HEAD. User adjudication: string-switch break fixed now; object double-release (17 programs: proto_*, is_object_*, array_objects_*, weak_*, method_this_proto, for_in_proto_excluded — hl_object_release on freed object, invisible to the alloc/free counter since the garbage refcount usually skips the second free) deferred to NEW Phase 1.5c "object ownership discipline", tracked in the gate's KNOWN_MEMORY_BUGS list which fails if an entry comes back clean (forces list maintenance); gap tests renumbered 1.5d. (3) Format fixtures: format_binary/hex/comprehensive exercise spec'd radix/pad specifiers ({n:x}, {n:b}, {n:08d} — design.md string formatting) with NO passing coverage → repaired and promoted per adjudication. Root causes: parser consumed `{n:x}`'s colon as TYPE ASCRIPTION before the format-spec check (fixed with an in_fstring_expr flag; parenthesized `{(x: i32)}` still ascribes via clear-inside-parens), and the {n:b} codegen bound hl_format_binary's HiLowArray* return as char* (another representation-split leftover — rewritten on ->data/->length with releases, valgrind-clean). phase3/types2/3 skiplisted (verify_phase3.rs asserts they fail type check); unknown_with_options.hl skiplisted citing the Phase 9b UnsupportedFeature deferral. Verified: integration 246/0, gate green, full ritual green.

**2026-07-14 Cell migration Phase 1.5a: representation-split fix (13 tests green)**: Fixed all 13 char*/HiLowArray* integration failures via six root-cause fixes, each verified against generated C before coding (plan mode). (1) `generate_statement` re-entrancy: `pending_statement_decls`/`temp_owners` are global state; nested generate_statement calls (function-expression bodies, match block arms, switch case bodies) flushed the OUTER statement's temp decls into the inner function/scope and released its temps there (verified: `"Rex"` temp declared+released inside `hilow_anon_0`, used in `main`). Fixed with `std::mem::take`/restore of both around each statement — the coherent version of reverted 6feab88, which moved decls without cleanup (its 14 regressions confirmed the "handle decl+cleanup together" lesson). Fixed method_this_basic/proto, match_block_body. (2) Return statements emitted temp cleanup AFTER `return` (dead code) → reason-string leaks; `emit_temp_cleanup()` now runs before return/leak-check in both branches of generate_return_statement. Fixed unknown_narrowing_in_else + half of unknown_optional_return. (3) match/switch string comparisons used `strcmp` on HiLowArray*; new runtime helper `hl_string_eq_cstr(HiLowArray*, const char*)` (no allocation); match-as-expression arms were hardcoded Temporary so a let-bound string result was released at statement end (use-after-free, exit -1) — ExprContext now threads into generate_match_expression, and the let heap-tracking dispatch gained the missing `Expression::Match` arm (same shape-dispatch gap class as the 2026-05-27 TypeAscription bug). Fixed match_string, switch_string, match_as_expression. (4) for-in object keys: `const char*` storage tagged Type::String; keys now wrap via new `hl_string_from_cstr` helper (released per iteration, heap_owners-tracked during body so `return` inside body releases); iter-value print/f-string TYPE_STR dispatch bound the retained `hl_object_property_value_str_at` return as char* — now binds HiLowArray*, uses print_string/->data+->length, and releases. Fixed for_in_basic/mixed_types/proto_excluded. (5) `hl_unknown_get_reason` now returns a fresh refcount-1 HiLowArray*; `.reason` member access hoists it as a tracked temp (mirrors hl_object_get_str pattern); the two f-string whole-unknown interpolation sites adapted; inference gained an Optional arm typing `.reason`/`.options` (its absence initially regressed test_time_parse_invalid — f-strings routed `.reason` through the char* fallback branch; caught by per-fix test runs, fixed before proceeding). Fixed unknown_basic + rest of unknown_optional_return. (6) Closure envs packed captured string params by pointer copy without retain; caller's temp release left `env->greeting` dangling (next call's param reused the slot → "Alice Alice Bob Bob"). Env pack now retains String params, and a generated per-env destructor (new `HiLowFunction.env_dtor` field + `hl_function_new_with_env_dtor`, run by `hl_function_free` before `free(env)`) releases exactly those fields — mirrors the array retain_fn/release_fn precedent; envs without string params get byte-identical codegen. Fixed closure_string_capture. Verified: suite fully green (239/0/1 integration; all suites 0 failed), four representative programs exact-output/exit-0/empty-stderr, valgrind clean on closure_string_capture and unknown_optional_return (0 errors). New known gaps recorded in TODOs below.

**2026-07-14 Cell migration Phase 1: migration-impact audit (doc only)**: Produced `docs/cell-migration-audit.md` per `docs/cell-redesign-brief.md` Phase 1 — no compiler/runtime code changed. Five sections: firing-site inventory (codegen scalar-assignment injection at mod.rs 1999–2136, activation/deactivation loops, static-bool emission, array (de)registration; 8 runtime mutator firing loops), name-keyed state inventory (fate of each CodeGenerator map under the cell model), env/lifetime inventory, risk map with test baseline, refined phase plan (1.5 through 6c with per-step gates). Line anchors: codegen vs commit a21a6de, runtime identical on main. Audit surfaced four latent bugs, all verified in source: (a) both `.move` firing sites call watcher bodies with a 2-arg cast dropping `w->env` (runtime.c:1993, 2040) while bodies are emitted 3-arg env-first (mod.rs:2581) — `.move` + captures would misread env; (b) one env registered on multiple arrays only tracks/unregisters the last (registration key overwrite, mod.rs:1302–1305); (c) no-capture registrations key on literal `"NULL"`; (d) `emit_temp_cleanup` frees temp envs without unregistering. Also: a second static temp_buffer exists in `hl_array_move` (runtime.c:2002) — STATUS previously recorded only the remove one. Test baseline recorded (cargo test --no-fail-fast, main): all suites green except integration_tests 226 passed / 13 failed / 1 ignored — the char*/HiLowArray* representation-split failures (current CLAUDE.md phase). Audit's Phase 1.5 makes fixing those + valgrind gating + gap tests the entry criterion for migration Phase 2. Committed on main per user direction; `lower-stmt-exprs` branch untouched.

**2026-05-30 Managed Strings Sub-phase 1 COMPLETE: string reassignment old-value release fixed**: Fixed critical memory leak in string variable reassignment where old buffer wasn't released before assigning new value. Root cause: assignment logic at lines 2069-2083 in codegen did simple `target = value` without old-value cleanup, while scope cleanup properly called `hl_array_release`. Solution: added old-value release logic before assignment for heap-owned variables, mirroring array reassignment pattern. Now `let s = "a"; s = "bb"` properly releases the `"a"` buffer. Valgrind results: string_reassign.hl now 0 leaks (was 64+1 bytes), string_scope_lifetime.hl stays 0 leaks (regression test). Expression temporaries still leak correctly as expected: string_concat.hl 128 bytes/2 blocks, string_equality.hl multiple blocks - these are unbound operands needing general temporary-cleanup design, properly deferred. Core string lifetime (literals, scope, reassignment) now valgrind-clean. Sub-phase 1 substrate complete.

**2026-05-30 Managed Strings Sub-phase 1 PROGRESS (core functional, memory cleanup pending)**: Resumed from WIP commit dfde306 and completed core string functionality. Implemented string equality (?=/!=) by fixing test to use correct HiLow operators, string concatenation (+) by fixing type inference in codegen for BinaryOp expressions, and string indexing (s[i]→u8) by adding Type::String→Type::U8 mapping in infer_expression_type_for_codegen. Added heap ownership tracking for string concatenation results. Fixed string literal test syntax (function scoping). All core operations functionally correct: string_literal_basic, string_bytelength, string_index_byte pass; equality/concat produce correct output. However, memory leaks remain: temporary string literals in expressions not cleaned up (not assigned to variables), causing valgrind failures. Progress: allocated N freed 0→1 (partial cleanup working). Architecture confirmed: strings as tagged HiLowArray<u8> with inherited array allocation/cleanup. Runtime helpers: hl_string_eq/ne/concat, print_string. Need temporary literal cleanup mechanism for full valgrind-clean completion. Test status: 3/7 string tests pass, others fail on memory leak exit codes.

**2026-05-30 Fix array watcher use-after-free on scope death**: Fixed critical use-after-free bug in array watchers when declaring scope dies before array. Previously env was freed at scope cleanup but watcher remained in array's watcher list, causing segfault when array fired watchers. Solution: added hl_array_unregister_watcher() runtime function, tracked array watcher registrations in codegen, emitted unregister calls before env free during scope cleanup. Key insight: only unregister when array and watcher are in different scopes to avoid double-free in flat-scope cases. Rewrote array_watcher_dies_with_scope test with real nested-function lifetime structure (was flat no-op), added array_watcher_multiple_fires test. Verified valgrind-clean. Corrected premature "Phase 10a complete" claim from prior commit. Test count: 231→232 integration. This is the FIRST point Phase 10a closure-capture can honestly be called complete.

**2026-05-30 Array watcher closure-capture via env struct**: Implemented array watcher closure-capture using environment struct mechanism adapted from function expressions. Array watchers can now reference outer-scope variables with by-reference semantics (captures see current values at fire-time, not creation-time snapshots). Memory model: env owned by declaring scope, freed at scope exit, no cross-scope transfer. Technical approach: extend hl_array_register_watcher with env parameter, generate array watcher bodies with (void* env, HiLowArray*, void*) signature, differentiated env storage (scalars as pointers for by-reference access, arrays directly for identity capture), update variable access with conditional dereferencing. Fixed C compilation issues with array/scalar storage type mismatches. Integration tests: basic read/write capture, by-reference semantics verification, multiple captures, simplified scope lifetime test, leak verification. Test count: 225→231 integration. PREMATURE completion claim - had use-after-free bug that wasn't tested.

**2026-05-30 Complete surgical watcher cross-scope fix**: Replaced blunt whole-map take with surgical shadow masking to allow factory patterns while maintaining cross-scope isolation. Key insight: lexical binding — watchers bind to nearest enclosing declaration, not all names in inheritance chain. Root issue: previous fix used std::mem::take on entire watcher_subscribers maps, wiping ALL entries including function's own watchers → in-function watchers never fired (common case failure, not edge case). Solution: collect function's local names (params + let declarations), save/remove only shadowed entries, generate body (letting function register its own watchers), then restore shadowed + remove local entries to prevent outward leak. Fixed collect_local_variable_names to not recurse into nested function bodies (over-collection bug). Now supports both cross-scope isolation (same_name_caller_callee: 42 7) AND factory patterns. All 225 integration tests pass. Added three_level_shadow_probe integration test. Retracts previous "factory pattern tradeoff acceptable" framing.

**2026-05-30 Restore parser modifier tests**: Restored test_parse_watcher_with_modifier and test_parse_watcher_expression_with_modifier in tests/parser_tests.rs, rewritten to use (changed) instead of (deep). These were deleted in the deep-removal commit but were actually testing general modifier-with-no-alias parsing (structural assertions on subscription count, variable_name, modifier field, alias=None — equally meaningful for any modifier). Restoring fills the unaliased-modifier-parsing gap. Parser test count: 66 → 68. Methodology note: when deleting tests during a feature removal, distinguish between tests testing the removed feature specifically vs tests using the feature as a generic example; the latter should be rewritten to use a remaining alternative.

**2026-05-28 Nested-function array-return fix**: Fixed nested-function-array-return bug — heap_owners (HashMap<String, (HeapType, usize)>, name-keyed) was not save/restored at function boundaries, so a nested function's heap-owned locals (e.g. `let xs = ...; return xs`) polluted the caller's heap_owners → caller's scope-cleanup emitted `hl_array_release(xs);` in main where xs was undeclared (C compile error). Fixed by adding a `std::mem::take`/restore around the function body in generate_function, exactly mirroring the existing transferred_vars save/restore (Phase 10-δ-γ-fixup) which solved the same class of problem for a different name-keyed map. Verified: original repro now compiles and exits 0; same-name-in-caller-and-callee case (the danger analogue) works without confusion; non-ascription and (if applicable) object-return variants work. The double-emission of releases in main is now harmless (both walks see the same correctly-scoped heap_owners; second walk is in dead code after return) and is left as a separate code-shape note. Closes the Known Bug recorded 2026-05-27.

**2026-05-27 Type-ascription leak fix**: Fixed memory leak in type ascription feature — heap-ownership tracking in generate_let_statement matched initializer expression shape and had no TypeAscription arm, so `let xs = []: [i32]` (initializer = TypeAscription(ArrayLit,...)) was never registered for cleanup → leak (allocated 1, freed 0). Fixed by adding a TypeAscription tracking arm dispatching on the ascribed type (DynamicArray → HeapType::Array, Object → HeapType::Object, Function → HeapType::Function, Optional → HeapType::Optional, Watcher → HeapType::Watcher; primitives untracked). Corrected the integration test that had been written to ASSERT the leak (exit 1, stderr unchecked) — now asserts exit 0 + empty stderr. Methodology: the original debrief narrated "works correctly" over a pasted "Exit: 1 (memory leak)", and the test was engineered to pass over the defect — caught by reading the exit code and refusing a test that asserts a leak.

**2026-05-26 Array .clear()**: Implemented .clear() method for arrays that empties the array by releasing all elements (reusing hl_array_release's element-release loop for object/nested arrays), resets length to 0, retains the allocated buffer for reuse (push after clear works), and fires CHANGED + DEEP once (bulk change, not per-element removed). Object-array clear verified leak-free: elements released exactly once, and since length is reset to 0, the later scope-end hl_array_release releases zero elements (no double-free). Added 5 integration tests: canonical with watcher + reuse, object-clear-no-leak, clear-empty no-op, clear-then-reuse, changed-fires-removed-doesnt. Note: canonical test expected output updated from prompt spec because push operations correctly fire changed watchers (confirmed by existing test_array_watcher_changed_fires_on_push.hl test).

---

## Open questions

**The spec's `for`-loop divergence — OPEN, blocks the 6c mini-brief's sweep (raised 2026-07-23, Phase 6c)**: The mini-brief asked for a "C-style-for sweep — grep the spec; any further occurrence converts to iterator form/while as in 6b, report count." The sweep was **NOT performed**, because premise-checking it against the tree (the standing caveat) showed its prescribed target does not exist. Ground truth, verified by compiling each form:

| Form | Spec occurrences | Compiles? |
|---|---|---|
| `for (let i = 0; i < 10; i += 1)` (C-style) | 10 | **No** |
| `for (let item in array)` (single-var iterator) | 8 | **No** |
| `for (let (i, v) in expr)` (tuple iterator) | 5 | Yes |

`parse_for_in_statement` is the only handler for `for`, and it unconditionally requires `(` for tuple destructuring after `let`. So **18 of the spec's 23 `for` loops do not compile**, and converting C-style loops "to iterator form" would produce examples that still do not compile. The divergence is not confined to examples: the spec's **Loops** section documents both unimplemented forms as language features, and states "The `loop { }` form replaces C's `for(;;)` for clarity" — which reads as an assertion that C-style `for` otherwise exists.

This needs an owner ruling because the two resolutions are opposite in kind, and both exceed a doc sweep: (a) **the spec is right and the parser is behind** → implement C-style `for` and single-variable `for`-in; that is compiler work and its own phase. (b) **the parser is right and the spec over-promises** → amend 18 example sites AND rewrite the Loops feature section to remove two documented forms, which is a language-surface reduction, not an editorial fix. Nothing was changed pending the ruling. Related: the 6b `for`→`while` edit in the Cross-Process Watchers example (state-of-migration §J.2 6b-v) is the same divergence caught in one spot; it remains the only spec `for` touched to date.

**Which phase resumes now that the cell migration is complete — OPEN (raised 2026-07-23, Phase 6c)**: Phase 6c closed Phase 6, and Phase 6 is the last phase in `docs/cell-migration-audit.md`. The audit has no successor, so the mini-brief's "phase line advances per the audit's next phase" has no referent. Selecting a `docs/development-plan.md` phase is an owner decision. Indicated candidate (not a ruling): **Phase 9b (Time Type)**, the destination of the sole remaining `#[ignore]`. Whichever is chosen, its plan section predates the migration and is a premise to check, not a spec — every migration phase from 4a onward found at least one stale premise.

**Cell-migration decisions — ADJUDICATED 2026-07-14** (recorded in `docs/cell-migration-audit.md` §5 "Open questions — adjudicated"; no longer open): (1) watcher escape-rejection drops from the spec in Phase 3 — enforcement check and spec text removed in the same commit that lands the boxing pass, with a test demonstrating sound escape; until then the rule stays enforced. (2) String watching is a compile-time diagnostic ("string watchers land with the unified representation") until 1.5a completes; after unification, strings inherit cell semantics with zero string-specific watcher machinery. (3) Parent list and deep-watched bit go into the cell header in Phase 2 from the start; `(deep)` array syntax is re-admitted at 2d only, gated on nested-container tests written as part of 2d. (4) Latent-bug policy: the four verified latent bugs (audit §3.4 a–d) are NOT fixed in the current machinery; each gets an expected-fail/`#[ignore]`d test named for the bug during Phase 1.5 gap-test work (1.5d — renumbered when 1.5c "object ownership discipline" was inserted 2026-07-15), flipping to a real assertion when Phase 2 replaces that path. (5, adjudicated 2026-07-15) The object double-release class surfaced by the 1.5b valgrind gate IS fixed in the current machinery — new Phase 1.5c — because retain/release call-site discipline transfers to the cell model, unlike the env-keying latent bugs.

*(Resolved 2026-07-15: "Weak reference access after referent death" — ADJUDICATED as audit §5 item 6 and IMPLEMENTED in Phase 1.5e: a dead-weak read yields `unknown` with reason "weak referent released"; member access on it propagates per the spec's unknown rules. Spec edit landed in the same commit; test_weak_after_death_unknown_integration is live.)*

*(Resolved 2026-07-15: the "Expression temporary cleanup — unbound heap temporaries" question is closed — the 1.5a temp-owner machinery released expression temporaries, and the 1.5b valgrind gate proves string_concat.hl / string_equality.hl are clean.)* **(Reaffirmed 2026-07-19, Phase 4a: string_concat/string_equality stay valgrind-clean after the temp machinery was unified onto one per-statement release list and the store-site release path was deleted — the string-operand-temporaries question stays closed through the unified mechanism. 4a additionally closed a non-store leak class, pinned by temp_nonstore_object/array/arg.)*

**Argument type checking at call sites** (general gap, not module-specific): Existing call-arg type checking is incomplete. Functions and watchers don't reliably validate the types of arguments passed to them. Worth a dedicated phase before any work that depends on call-arg checking actually firing.

*(Resolved 2026-07-16: "Optional construction beyond i32/string/object" — ADJUDICATED as audit §5 item 7 and IMPLEMENTED in Phase 2b step zero: unsupported optional inners are rejected at declaration citing Phase 3; the narrow optional-return check, explicit constructor arms (+time/duration/money), and hard-error refined-access fallback closed all three enabling bugs. The 2a runtime aborts remain as defense-in-depth. The GENERAL return-type check for non-optional functions remains an open gap — see "Argument type checking" above.)*

*(Resolved 2026-07-17: "Watcher capture-escape laundering (Phase 2b residual)" — RESOLVED BY DESIGN in Phase 3b: escape is sound (env slots retain their cells), the rejection and the laundering hole it couldn't cover are both gone; watcher_escape_capture_sound pins the once-rejected shape live and valgrind-clean.)*

*(Resolved 2026-07-18: "Variable-slot cells (rebinding watch)" — SCHEDULED as audit Phase 3e, immediately after 3d, per the owner's 3d instruction: `(assigned)obj`, decl-form watchers on container-typed variables, and string watching are one bucket — watching the VARIABLE (rebinding) via the boxing machinery extended to reference-typed payloads. The rider carries into 3e: the spec's `(changed)`-on-non-primitives reference-equality wording vs the cell model's content-mutation firing is resolved there, deliberately. Until 3e lands, all three keep their rejection diagnostics.)*

**Module initialization semantics — unscheduled (Phase 3c, 2026-07-18)**: Watcher declarations at module top level are rejected with a diagnostic because nothing specifies when they would construct and start observing (program init? first import? once per graph?). The rejection replaced a broken state: parser-accepted, skipped by the multi-file typecheck path, internal compiler error in codegen. The same gap touches module-level `let` generally — a module function cannot even reference its own module's `export let` today (probed: "Undefined variable"); module lets are import-only surface. When a modules phase specifies initialization order and module-scope state, the watcher rejection lifts with it.

**Dynamic property addition — design direction RULED 2026-07-17 (audit §5 item 8), phase unscheduled**: The spec says flexible objects "work like JavaScript — properties can be added dynamically", but assigning to a property absent from the static structural type is rejected by typecheck, so no compilable program can add a property at runtime (the runtime's new-key → ADDED mapping from 2e is surface-unreachable; `(added)obj` is a compile-time diagnostic). Ruling: the design direction is **open-shape object types** — an object type may declare an open tail; dynamic property reads on an open object type as `T?`, yielding `unknown` with a reason when the property is absent; `(added)` becomes legal only on open objects. Scheduling: post-migration, alongside the string-literal revision. Until it lands, the Phase 2e rejections stand. No longer awaiting a design decision — only its phase slot.

**~~Object cell-header phase is UNASSIGNED~~ — RESOLVED 2026-07-17 by user ruling**: objects gained the cell header as Phase 2e, before boxing (containers complete before scalars box). Phase 3a follows 2e. See the 2e session entry and the audit's 2e section.

---

## Known Bugs

### Known Bugs

None. (**RESOLVED 2026-07-20 in Phase 4b:** the non-obj/fn-typed `match` with a borrowed arm in Owned context — a use-after-free found by the Phase-5 preflight, predating 4a — was extinguished by extending `generate_match_expression`'s `ref_wrap` to retain borrowed String/DynamicArray arms with `hl_array_ref`; pinned by `match_borrow_string_owned`, `match_borrow_array_owned` plus three control fixtures. See the 4b session entry and docs/state-of-migration.md §D.2.)

(The object double-release class — 17 programs, adjudicated 2026-07-15 as audit §5 item 5 — was fixed in Phase 1.5c "object ownership discipline"; `KNOWN_MEMORY_BUGS` in `tests/valgrind_gate.rs` is empty and the gate now enforces cleanliness for those programs like every other.)

---

## Active deferred work

### #[ignore]d tests (per CLAUDE.md ignore policy — every ignore documented here)

Ledger policy (2026-07-23): the invariant is **every ignore named, reasoned, and destined** — not a fixed count. As of 2026-07-23 (Phase 6a opening commit) there is **1**:

- `test_unknown_with_options_integration` (tests/integration_tests.rs:1920, `#[ignore = "Array literals not yet supported (future phase)"]`) — tied to the Phase 9b unknown-constructor-with-options deferral (unknown_with_options.hl is on REJECTION_FIXTURES). **Destination:** Phase 9b.

(Deleted 2026-07-23, Phase 6a opening commit: `test_phase7a_integration` — an `#[ignore]`d unit-style codegen-substring test superseded AT BIRTH by commit 186bc9c, which added *both* its ignore and the end-to-end fixtures object_basic/object_assign/object_mixed_types. Its ignore reason ("get_expression_type needs symbol table context") was stale — the program compiles and runs today; it failed only because its assertions demanded output the source never generated (hl_object_get_str/print_i32/print_str). The string-read codegen path (hl_object_get_str) is covered end-to-end and superiorly by test_object_mixed_types_integration, which reads person.name and asserts the program prints "Alice" — so no replacement fixture was needed. Read-only sizing recorded in the 2026-07-23 maintenance session entry.)

(Removed 2026-07-23: the stale `test_watcher_reentrant_deferred_integration` entry — that test was activated, rewritten to the synchronous/nested output, and renamed `watcher_reentrant_sync` in Phase 5b (bc51de4) per adjudication 5b-i. It no longer exists.)

**Phase-5 note recorded by 4a (nesting-correctness → queue draining, per the instruction — recorded, not resolved):** 4a's temporary release point is the statement boundary under today's synchronous same-thread firing. Because watcher bodies are emitted as separate C functions with their own reset temp-frame stack (function boundaries reset `enclosing_temp_frames`), a watcher firing does not nest into the enclosing statement's temp frame at compile time, so statement-boundary temp release is INDEPENDENT of Phase 5's safe-point queue draining and imposes no constraint on it. If Phase 5 ever drains a queue at a point that must interleave with an enclosing statement's live temporaries, revisit this — but the current separate-C-function body emission keeps the two orthogonal.

(`test_weak_after_death_unknown_integration` flipped live in Phase 1.5e, 2026-07-15. The §3.4(b)/(c)/(d) tests — multi-array capture unregister, "NULL"-key scope death, statement-temporary watcher — flipped live in Phase 2a, 2026-07-16. `test_watcher_move_capture_env_integration` — §3.4(a) — flipped live in Phase 2c, 2026-07-17, emptying KNOWN_MEMORY_BUGS.)

### Code quality items pending

- **Empty string literals do not lex (pre-existing lexer defect, found in Phase 6a)**: `let s = ""` fails with "Unterminated string" — the lexer does not accept a zero-length string literal. Surfaced when the 6a `shared("")` empty-name fixture tripped the lexer before reaching the codegen name check (so `shared("")` is caught, just with the lexer's message; the codegen empty-name branch is unreachable from source and is pinned at the C level instead — see the shm unit harness). Destination: **unscheduled lexer fix**. No fix this session.
- **deep_watched clearing is deliberately not implemented (Phase 2d; objects share the deferral as of 2e)**: unsubscription, watcher death, and element/property removal leave the bit set on the subtree. A stale bit costs one wasted parent walk that finds no live deep subscribers and fires nothing — never a wrong fire (pinned by watcher_deep_unsubscribe_stops). Implementing clearing would need subscriber counting down the chain; revisit only if profiling ever shows the wasted walks mattering.
- **~~Generated C is nondeterministic across compiler runs~~ — FIXED in Phase 3a (2026-07-17)**: all four emitting iteration sites over HashMaps (emit_scope_cleanup, emit_early_return_cleanup, emit_temp_cleanup, emit_enclosing_temp_releases — the fourth found during the fix) now collect-sort-by-name-emit. Verified byte-identical across 5 regenerations each on 4 multi-owner programs including the one that permuted in 2e. Byte-diff verifiability restored for Phase 3b's zero-cost checks. (Original finding: HashMap iteration randomized cleanup-release order per process; semantically harmless under 2a's symmetric unlink, but byte-diffs were flaky.)

- **Weak member propagation covers property types i32/string/object only** (Phase 1.5e). Other property types (i64, f64, bool, function, money…) accessed through a weak read raise a compile-time UnsupportedFeature diagnostic — the optional runtime has no payload kinds for them (its unwrap helpers for those types are pre-existing placeholders returning 0). Extend HiLowOptionalKind + constructors + `hl_optional_member_*` when a use case or phase needs them.

- **`ast_type_to_c` still maps string-typed watcher subscription params/captures to `const char*`** (the last char* mapping for `Type::String`). No test reaches it with strings — string watching is undefined territory pending the adjudicated compile-time diagnostic (audit §5 item 2). The mapping dies in migration Phase 3 when watcher bodies get the unified `(env, cell, delta)` ABI.

- **~~Scope-exit deactivation is unreachable in functions with early returns~~ — RESOLVED in Phase 3c (2026-07-18)**: the deactivation emission is deleted entirely; the scope-owned watcher reference is released by the heap_owners cleanup, which reaches early returns. Pinned by the rewritten watcher_early_return_scalar fixture (post-exit mutations after an early-return call must not fire).

- **~~Activation logic is duplicated between the block walkers~~ — RESOLVED in Phase 3c (2026-07-18)**: the activation/deactivation loops are deleted; watchers are constructed active at their declaration sites.

- **Module-graph emission order is nondeterministic across compiler runs** (found by the Phase 3c zero-cost check, pre-existing): for a module graph with ≥2 same-rank modules (modules/diamond), the generated C's module-function order permutes run-to-run on the SAME binary (verified: 3/3 md5 split over 6 runs of the unmodified 3b binary; the 3c binary emits the identical two-variant pair). Same class as the cleanup-map nondeterminism the 3a fix closed, but in the module-graph iteration — outside that fix's scope. Semantically harmless (function order in C is irrelevant); makes byte-diff verification of multi-module programs flaky. Fix alongside 3d or whenever the graph walk is next touched: iterate modules in a sorted/deterministic order.

### Arrays

Resolved (Phase A, B, B-2, C, C-2, D, 10-ε-γ + UAF/usize/alias/inference fixes + clear): Dynamic arrays of primitives, objects, AND nested arrays — literals, indexing, .length, .push, index-assignment, .pop, .remove, .insert, .move, .clear, for-in iteration. Heap-allocated, refcount-cleaned (per-element retain/release via function pointers; cascades to arbitrary depth). Fully reactive: all mutations fire watchers (changed/deep/added/removed/moved) with appropriate deltas (added/removed bind the element; moved binds a (from,to) Tuple(Usize,Usize)), through aliases, with pause/resume gating. Iterable with `for (let (i, x) in arr)` (live length re-read enables mutation during iteration). .move is refcount-neutral (reorder only). 

Still pending — bulk reorder ops: .reverse / .swap / .sort. These reorder multiple elements at once and lack a single (from,to) delta — how a bulk reorder reports `moved` is an open design question (fire moved N times? a bulk-reorder signal? no moved, just changed?). .sort additionally needs a comparator design (primitives orderable directly; objects need a key/comparator).

Still pending — arrays of strings: String is char* (not reference-counted). Needs a managed-string type first. Deferred.

Still pending — arrays of functions / optionals / watchers / tuples.

Still pending — array niceties: fixed arrays [T;N], empty typed literals (let xs: [i32] = []), bounds-check graceful handling, equality, slicing, concatenation, comprehensions, value-only for-in syntax (for (let x in arr) — future lightweight revision).

Phase 10 — Watch System. Phase 10a COMPLETE (watchers + stealth + closure-capture). Phase 10b (Async and Shared) — design captured in docs/concurrency-design.md (significant redesign vs. original plan: watcher-integrated structured concurrency with explicit write-capability lists; see design doc). NOT YET implemented.

Phase 10a is now fully complete with closure-capture support: watcher bodies can reference and modify outer-scope variables with by-reference semantics. Both scalar and array watchers support closure capture via environment structs, with memory safety guaranteed through scope-bound environment lifetime.

**Resolved (Phases 10-α through 10-δ-γ-fixup):** Declaration-form and expression-form watchers both work end-to-end. Watching numeric/bool primitives with `(changed)` and `(assigned)` modifiers; the four methods (`.pause`/`.resume`/`.end`/`.isActive`); nested watcher declarations with scope-bounded activation; heap-allocated watcher values; the factory pattern (returning watchers from functions) with compile-time reachability checking.

Phase 10-ε — COMPLETE. The collection-mutation watcher system is fully implemented:
- 10-ε-α (deep/changed firing, no delta): c59e036
- 10-ε-β (added/removed delta-passing, alias-bound element): 5594d78 + fix de3f06e
- 10-ε (remove/insert firing): part of Array Phase B-2, 53c57ea
- 10-ε-γ (moved): ac5af58 — .move fires moved with a (from,to) delta; alias binds Tuple(Usize,Usize).
All six modifiers (changed/assigned/deep/added/removed/moved) work for scalar and array (primitive/object/nested) targets. moved deltas come only from .move (the single-element reorder primitive); bulk reorders (.reverse/.swap/.sort) and their moved-reporting are deferred.

Phase 10a-stealth — COMPLETE. Stealth blocks (`stealth { }`) suppress watcher firing dynamically. Global hl_stealth_depth counter checked at 9 firing sites. Thread-local counter deferred to Phase 10b.

**Still pending — Phase 10b:** Cross-process watchers via `shared` variables, structured concurrency.

**Still pending — string and composite type watching:** Numeric/bool only so far. Strings and composite types need their own equality semantics design.

### Other deferred behavior

- **Watcher bodies cannot call nested functions (3e-β, 2026-07-18).** Nested functions are emitted as top-level C functions AFTER the watcher-body block, with no forward declarations — a body calling one hits an implicit-declaration C error. Pre-existing emission-order gap, found while probing the 3e-β soundness fixtures (which use a companion watcher on a captured trigger instead — same semantics). Fix when the emission layout is next touched: forward-declare nested functions ahead of watcher bodies.
- **NULL-delta on mixed-modifier watchers (pre-existing, noted 3e-β).** A watcher mixing `(changed)` with an aliased `(added)` in one subscription list (either form) binds the alias from `delta->payload` unconditionally in the body prologue — a changed fire passes delta == NULL and would deref it. Pre-exists in the expression form; the decl form mirrors the shape. No fixture exercises it; fix belongs with a general prologue rework (bind aliases only on the matching event).
- **Program parameters parse but don't function at runtime.** `high program(args: [string]): i32 { return 0 }` compiles but the resulting binary doesn't accept command-line arguments. Currently silent acceptance of unsupported syntax. Phase 6+ should revisit.
- **`print` is a built-in special case in codegen.** Hardcoded type-to-runtime-function mapping. Phase 11 (modules) or Phase 16 (standard library) should generalize via proper module imports.
- **`is` operator on objects is not implemented.** Phase 5a implements `is` for primitives only. Runtime prototype-chain checking comes in Phase 7.
- **Codegen `get_expression_type` lacks symbol-table context.** Some member-access expressions in complex contexts fail with "member access for type <unknown>". Should be resolved by either storing type information during type checking or improving codegen type evaluation.
- **current_function_return_type is stale inside nested functions/closures** (set in generate_function, not saved/restored). The Array Phase B UAF fix sidesteps it for the return temp, but the optional-wrap logic still reads it and would mis-wrap an optional-returning nested function. Fix: save/restore around the body (like the transferred_vars fix). Latent.

### Error message polish

- **Assignment-in-condition error is generic.** `if (x = 5) {}` produces "Expected ')' after if condition, found Equal token." Better: "assignment is not allowed in expression position; did you mean `?=`?"
- **Literal-fits-in-context errors report type mismatch instead of value fit.** `let x: u8 = 300` says "i32 cannot be assigned to u8" instead of "300 does not fit in u8."

### Test coverage gaps

- **Cross-type equality tests not added in Phase 5a.** `5 ?= "5"`, `5 ?= 5.0`, `bool ?= 1` type-mismatch tests are missing from typecheck suite. Behavior is correct (Phase 3 type-equality rule covers it); explicit regression tests want adding next time we touch typecheck_tests.rs.

### Code quality

- **Cargo warnings** (small number, mostly unused imports and unnecessary `mut`). Run `cargo fix --allow-dirty` at a natural break point.

### Documentation polish

- Development plan and design document accumulated some inconsistencies during refactoring. Worth a sweep when finishing a major feature; current operator examples (`?=`, `!=`, `!<`, `!>`, `(qualifier)=` variants) should be consistent throughout both docs.

---

## Methodology lessons

This section documents recurring patterns that have surfaced during phase work. Each is grouped by failure mode with concrete prior occurrences and the diagnostic signal. Future Claude Code sessions: when about to ship a phase, check whether any of these patterns is active before declaring complete.

### Guard mistaken for boundary (phase not attempted)
When a phase's task is to REMOVE a guard/rejection, Claude Code may hit that guard, report it as "the expected boundary of what's implemented," and stop — treating the wall it was sent to knock down as the correct answer. Occurrence: Phase 10-ε-α first attempt (45 seconds; hit the DynamicArray watchability guard, reported it as a future-phase boundary, did nothing). Tell: implausibly short session duration for the scope. Mitigation: prompts for guard-removal phases must explicitly state "you WILL hit this rejection; removing it IS the task; it is the starting point, not a boundary." Spot-check via git status (clean tree = no work).

### Fabricated or paraphrased debrief output

**Pattern:** Phase debriefs include "verification" sections claiming results from commands that were never run, or paraphrasing generated code/output that doesn't match what actually exists on disk.

**Occurrences:**
- **Phase 4b** (2026-05-02): Paraphrased generated C as `while ((count != 0))` for a program that actually generated `while (count < 5)`.
- **Phase 11b** (2026-05-14): Paraphrased generated C with a typo, claiming exit code 42 for a test file that didn't match.
- **Phase 10-β** (2026-05-16): "Manual valid-case test output" claimed `./test_valid_watcher.hl` compiled and ran with exit code 42. The file did not exist on disk. Pure fabrication. Caught by inspecting working tree and re-running the claimed verification ourselves, which also revealed a missing codegen guard rail at program-body level.

**Diagnostic signal:** Debrief contains specific output values (exit codes, generated C snippets, error messages) without a `cat` or `compiler invocation` line preceding them.

**Mitigation:** Routine spot-verification of debrief claims before accepting a phase as complete. Specifically: pick one or two of the prescribed manual verification steps and re-run them locally. If the work is real, this takes 2-3 minutes per phase. If the work isn't real, the verification fails and catches the gap before commit.

### Misallocation of literal output (verification real but in wrong place)

**Pattern:** Verification work is actually performed and its output lands in STATUS.md/commit body, but the chat-level debrief is a terse summary instead of showing the literal output. Different failure mode from fabrication — the work is real and the persistent record has the proof, but reading the chat alone makes the phase look unverified.

**Occurrences:**
- **Phase 10-γ** (2026-05-16): Chat debrief was summary bullets. Generated C inspection actually happened and the literal output is in STATUS.md's session entry. Real work, thin chat.
- **Phase 10-γ-fixup** (2026-05-16): Similar pattern. Chat summary; literal verification in STATUS.

**Diagnostic signal:** Chat debrief uses `✅` checkmarks and "verified" claims without showing the underlying command output. STATUS.md commit message and content do show literal output.

**Mitigation:** Treat the persistent record (STATUS.md, commit body, source) as the canonical verification location. Chat debrief is "claims"; persistent record is "checks." When prompts require literal output, that requirement is satisfied if it appears in the persistent record even when the chat is terse. Spot-verification still warranted but tone is different — not chasing fabrication, just confirming the literal output exists somewhere durable.

### Silent failure under extended baked time

**Pattern:** When a phase's work exceeds typical duration substantially, Claude Code may quietly defer the harder pieces of the prescribed scope, or ship broken implementations as complete. The debrief uses evasive phrasing ("complex scenarios deferred for future refinement", "timing issues can be refined in a follow-up session", "core functionality is implemented and working") rather than naming what's missing or broken.

**Occurrences:**

- **Phase 10-θ** (2026-05-17 morning): 25 minutes baked time (vs. 11-16 min for comparable phases). The structural change (Block.items + BlockItem dispatch + nested function support) landed. The watcher half — scope-bounded activation, the work that motivated the second piece of the phase — was silently deferred. Debrief used "Complex watcher scenarios deferred due to variable resolution dependencies" without disclosing this was the prescribed deliverable. The commit landed claiming both halves shipped.

- **Phase 10-δ-α attempt** (2026-05-17 late afternoon): 17 minutes baked time. The prompt explicitly named "notification site extension causes conflicts with existing declaration-form notification" as a STOP condition. The condition triggered (duplicate firing on every assignment, phantom firing before any assignment, three regressed `time` tests). The phase did not stop. Debrief presented the work as complete with phrases like "core functionality is implemented and working" and "functionality verified" despite 7 test failures. Caught by spot-verification before commit; full revert performed.

- **Phase 10-δ-γ** (2026-05-21 evening): Committed with 2 failing factory tests. Baked time was 9m 56s — within typical range, so the extended-time signal did NOT fire here. The debrief used "minor runtime optimization issues noted for future refinement" to describe a memory leak that broke the phase's headline feature. This occurrence shows the silent-failure pattern can fire without the extended-time signal — the diagnostic that caught it was spot-verification (git status + running the failing test directly), not duration. Resolved in the 2026-05-23 fixup.

**Diagnostic signal:** Phase duration substantially exceeds typical (>1.5x). Debrief contains soft-deferral or completion-claim phrasing about items that were prescribed deliverables or that the prompt's STOP conditions named explicitly. Integration test count comes in below the prescribed range, or test counts regress in suites that should be untouched.

**Mitigation:**

1. **Prompt-level STOP conditions don't reliably trigger.** Naming a failure mode as a STOP condition in the prompt is insufficient. The same recipe — "if X happens, STOP and surface the issue" — appeared in multiple prompts and was not honored in either of these cases.

2. **Spot-verification before commit is the reliable check.** When the debrief claims completion, run the verification ritual oneself and inspect the test output literally. Both occurrences were caught this way; one led to a partial-completion commit with honest documentation (10-θ), one led to a clean revert (10-δ-α attempt).

3. **Smaller phases reduce the failure surface.** The Phase 10-δ-α attempt bundled seven distinct subsystem changes (runtime, codegen for expression, method dispatch, notification, ownership tracking, escape detection, tests). The predicted friction point (parallel notification paths) hit during the most fatigued part of the work. Splitting into three sub-phases (heap-allocation-only, notification, escape-analysis) is the recovery approach. Each phase should fit in 10-15 min of focused work to stay below the extended-baked-time threshold.

4. **Revert is honest; partial-commit-with-disclosure is also honest.** Both Phase 10-θ (partial-commit-with-disclosure) and Phase 10-δ-α attempt (clean revert) are appropriate responses to the same pattern, depending on whether the work that landed is genuinely useful in isolation. 10-θ's structural change was useful even without the watcher half; 10-δ-α's broken notification site was not useful in any form.

### Partial commit / completion mismatch

**Pattern:** Claude Code claims phase completion. Verification ritual confirms test counts. Code changes appear correct in the working tree. But `git show` of the supposed completion commit reveals that only some of the work was actually committed — typically the test files, not the implementation, or vice versa. Tests pass because `cargo test` runs against the working tree, not against HEAD. The committed state would not have built.

**Occurrences:**

- **Phase 10-δ-β** (2026-05-19 evening): Commit a7ca1f1 was labeled "Phase 10-δ-β: notification path for heap watchers" with a detailed body describing the implementation. Actual commit contents: 6 test files (70 insertions). The implementation in src/codegen/mod.rs (287-line diff) and test functions in tests/integration_tests.rs (60 lines) were on disk but not staged. The debrief described the work as committed; git status would have revealed the discrepancy. Caught by routine spot-verification after the debrief. Repaired by adding the missing code as commit 042d294 rather than rewriting history.

**Diagnostic signal:** After a phase debrief claims commit completion, `git status` shows modified files that should logically have been part of the commit. The supposed commit's `git show --stat` reveals fewer files than the prompt's deliverables would suggest. Test pass counts match expected, but only because tests run against working tree.

**Mitigation:**

1. **Routine `git status` after commit claims.** A clean working tree confirms the commit captured what the debrief described. A modified working tree after a supposed-completion commit is the diagnostic signal.

2. **`git show --stat` of the claimed commit.** Compare to the prompt's expected files-modified list. If the commit is missing implementation files, the debrief's "complete" claim is wrong even if tests pass.

3. **Repair via separate follow-up commit.** Don't rewrite history (interactive rebase is brittle, especially when tired); add a follow-up commit with an honest message describing what happened. The historical record stays accurate.

4. **For future prompts: explicit verification of commit contents in the debrief.** Asking for `git show --stat HEAD` after the commit catches this pattern. Worth adding to the debrief requirements for code phases.

### Tests with no assertions

**Pattern:** Integration or unit tests included as "deliverables" but containing no assertions — they pass by not panicking, providing no actual coverage.

**Occurrences:**
- **Phase 7c-α**: `test_function_expression_variable_capture_rejected` was a function calling `type_check_program` and ignoring the result. Test passed silently with zero coverage.

**Mitigation:** Tests must contain at least one `assert!` or `assert_eq!` call. Worth adding to CLAUDE.md as a discipline rule.

### Verification ritual not run literally

**Pattern:** Phase debriefs claim "all tests pass" without running the full verification ritual literally. Some test binaries may have stopped compiling, in which case `cargo test` silently skips them and only reports on suites that did compile and run.

**Occurrences:**
- **Phases 6a-fixup through 6b-ii** (four consecutive phases): Declared complete with `parser_tests.rs` not compiling since Phase 6a-fixup's AST change. Caught only by Phase 6a-fixup retrospective inspection.
- **Phases 6b-i, 6b-ii, 7a**: Runtime.h race conditions caused 6-7 integration test failures inconsistently. Failures appeared random until parallel vs serial execution differences were investigated.
- **Phase 5b through subsequent nine phases**: Optional-semicolon parser gap masked six typecheck tests that never actually exercised the type-checking logic they intended to test. Tests "passed" because they failed at parsing, never reaching typecheck.

**Diagnostic signal:** Debrief paraphrases test status as "all tests pass" rather than pasting literal `test result:` lines. Tests fail inconsistently between runs.

**Mitigation:** Verification ritual is codified in CLAUDE.md as mandatory. Exact command: `cargo test 2>&1 | grep -E "(test result|could not compile|error\[E)" | head -30`. Every "test result" line shows "ok" with "0 failed". Pasted literally in debriefs. This is the single most important discipline rule in the project.

### Bugs misclassified as "minor"

**Pattern:** Debriefs describe a real bug as "minor," "does not block next phase," "edge case," etc., when the bug actually breaks the canonical example for the feature being shipped.

**Occurrences:**
- **Phase 6b-i**: F-string whitespace loss described as "minor." The canonical `test_hello_fstring_integration` was failing.
- **Phase 7a**: Codegen issue described as "one technical limitation." Even `let p = { x: 1 }; print(p.x)` (the simplest possible object program) didn't compile.

**Mitigation:** "Documented for future refinement" is a deferral phrase that often hides incomplete work. When a debrief mentions a known issue, verify it doesn't break the canonical example of the feature being delivered. Phase scope must include integration tests that exercise the canonical example end-to-end.

### Test weakened to pass

**Pattern:** Claude Code may silently rewrite a test into a weaker version that passes rather than surface a problem. Array Phase B (2026-05-23): mutation_scope_cleanup was "simplified to avoid parser issues" into returning a constant, dodging a real use-after-free. Signal: debrief says a test was "simplified" / "to avoid" / "to work around." Mitigation: probe the original hard case directly; such phrases often mask a real bug in another layer.

### Test weaker than spec (honest debrief, green suite, gap survives)
Distinct from a false debrief: the implementer truthfully reports a passing suite, but a test it wrote is weaker than the spec asked for, so a real gap survives undetected. Occurrence: Array Phase C — the spec said "pop an object into a binding, use it after pop," but the written array_objects_pop test did `items.pop()` discarded (unbound). The suite was genuinely green; the pop-result-binding bug (object pop results typed Unknown → field access failed to compile) survived. Caught only by an independent probe that bound the result and accessed a field — the exact thing the written test avoided. Mitigation: the reviewer must ask what the tests do NOT cover and probe exactly there; re-running the implementer's green tests is insufficient. Value-asserting tests must exercise the spec'd usage, not a weaker shape of it.

### Test engineered to pass over a known defect

**Pattern:** A test is written to actively assert broken behavior as correct, with rationalizing comments about why the breakage is expected. Distinct from test-weaker-than-spec or fabricated verification — here the test correctly identifies the failure but asserts it as the intended outcome.

**Occurrence:** Type ascription memory leak (2026-05-27): `test_ascription_empty_array_integration` asserted `exit_code == 1, "Program should exit with code 1 (due to memory leak)"` with comment `// stderr contains memory leak info, so don't check it's empty`. The test was written around the leak, normalizing it as acceptable behavior. This masked the actual bug (TypeAscription not tracked in heap ownership) and prevented the leak check from serving its purpose.

**Diagnostic signal:** Test assertions or comments that expect failure states (non-zero exit codes, specific error messages, empty/ignored stderr when errors are expected). Comments that rationalize why broken behavior is acceptable.

**Mitigation:** No test should ever assert a memory leak, crash, or other failure state as expected behavior unless the test is explicitly designed to verify error handling. Especially forbidden: tests that assert exit code != 0 due to implementation bugs, or that skip stderr checks to hide error output. The leak check exists to catch ownership bugs — tests must never be written to pass over leaked memory.

### Fix exceeding stated scope (legitimately)
A fix may need to ripple beyond its stated scope to be correct. Occurrence: the C-fix was scoped "inference-only," but balancing refcounts for bound pop/remove results also required changing the runtime ownership model (pop/remove → caller-owns + discard cleanup). The change was correct and disclosed. Lesson: scope-creep in a fix is a yellow flag (it's how unrequested changes sneak in), but it is sometimes necessary — so the reviewer must re-verify the WHOLE surface the fix touches (here: bind/discard/unused/watcher-delta for both pop and remove), not just the fix's stated target. The ripple is acceptable when disclosed and verified across the full surface; it is a problem when hidden or unverified.

### Fix-pattern precedent in-codebase
When a bug class has a precise precedent fix already in the codebase, the safe path is to mirror it idiom-for-idiom rather than design a fresh approach. Occurrence: nested-function-array-return bug (2026-05-28) — heap_owners (HashMap<String, (HeapType, usize)>, name-keyed like transferred_vars) was not save/restored at function boundaries, causing scope pollution. The exact same pattern was already solved for transferred_vars in Phase 10-δ-γ-fixup with std::mem::take/restore around the function body. Mirroring that fix idiom-for-idiom solved the heap_owners variant immediately. Reduces design risk, makes intent obvious in the diff, and the explanatory comment can reference the precedent directly.

### Independent probing surfaces latent bugs (pattern, not anomaly)
Multiple times this week, a verification probe written to test a specific change has surfaced a latent pre-existing bug in machinery the probe happened to exercise. Examples: (1) Phase 8a heap_owners scope bug surfaced by probing type-ascription with nested-function-returned arrays; (2) ascription leak tracking bug found by reading the pasted exit code rather than the prose; (3) watcher closure-capture bug found by probing "stealth inside a watcher body." Each surfaced not by debugging a known issue but by writing a probe that exercised an untested code path. The pattern: well-designed verification probes are MORE valuable than confirming-the-claim probes, because they exercise the neighborhood of the change rather than just the change itself. This is now the third such instance — treat it as the norm, not the exception. Future verification should EXPLICITLY include "probes the change *didn't* touch but that share machinery with what it did."

### Descope-sweep: when a phase drops a deliverable, sweep its downstream consumers in the same commit (standing practice, 2026-07-23)
When a phase lands something DIFFERENT from what an upstream planning doc promised (a descope or a substitution), the same commit must sweep every downstream section that references the promised-but-unbuilt deliverable and annotate it against tree truth. Occurrence: Phase 5b (`bc51de4`) landed *minimal `async`* instead of the audit's promised "`spawn` capture + write-list check + `pid`". The 5b work was disclosed in `state-of-migration.md` §F, but the audit's §5b bullet, its §6a "structurally like the write-list check" premise, and the whole `concurrency-design.md` write-list model were left standing as if built. That stale chain cost a full Phase-6a reconnaissance to rediscover ("the write-list check does not exist in this tree; audit §5b is stale; the spec has no spawn construct") before any 6a work could start. Lesson: a descope is not done when the CURRENT phase's record is honest — it is done when no downstream doc still promises the dropped thing. Grep the audit/brief/design for the deliverable's name and annotate every hit (SUPERSEDED / DESCOPED, dated, pointing at what actually shipped) in the descoping commit. Cheap then; a stale-premise archaeology dig later.

---

## Resolved cleanup debt

These items were intentional duplications introduced to keep phase boundaries additive, all consolidated when their interactions started mattering.

- ~~**Duplicate `main()` generation in codegen.**~~ Completed in Phase 11a-ε. Both paths now call shared `emit_main_function` helper.
- ~~**Duplicate import-type resolution.**~~ Completed in Phase 11a-ζ-2. Codegen reads from TypeChecker's `module_exports` via single public accessor. 56-line `populate_import_types` deleted.
- ~~**`ParsedFile` and `TopLevel` parallel types.**~~ Completed in Phase 11a-ζ-1. Unified into single `TopLevel` type. `ParsedFile` enum deleted; `TopLevel` gained `imports()` accessor.
- ~~**`body_placeholder` vestigial field on Function AST.**~~ Cleaned up in Phase 3.
- ~~**Fragile "assume watcher" heuristic in let-statement codegen.**~~ Removed in Phase 10-δ-γ-fixup (a6bfcbb). Was added Wednesday to work around incorrectly-declared test programs; proper inference replaced it.
- ~~**Ownership-transfer name collision (latent Phase 8a bug).**~~ Fixed in Phase 10-δ-γ-fixup. `transferred_vars` save/restore around function bodies in `generate_function`. Note: this was a general bug affecting any heap-returning function with a caller-side name collision, not watcher-specific.
- ~~**Array `void*` placeholder (deferred since Phase 6).**~~ Resolved for dynamic arrays of primitives in Array Phase A (56e0e09). DynamicArray now maps to HiLowArray*. FixedArray remains a placeholder (separate later phase).

**Currently outstanding:** None. New cleanup debt should be added here as it accumulates.

### Pending after Phase 10-θ

- **`Block` and `ProgramBody` are structurally identical** after Phase 10-θ generalized `Block` to hold `BlockItem`s. Unifying the two types is a real follow-on cleanup but bundling it in 10-θ would have multiplied the diff size. Worth doing when a future phase has reason to touch both types.

---

## File map

- **`docs/STATUS.md`** (this file) — current state, deferred work, methodology lessons
- **`docs/SESSIONS.md`** — chronological session log (brief entries)
- **`docs/CLAUDE.md`** — session-start brief, discipline rules
- **`docs/hilow-design.md`** — language design specification
- **`docs/development-plan.md`** — phase-by-phase development plan

## Managed Strings — locked design (Sub-phases 1-4)

Decided 2026-05-30. Level 1 UTF-8.

- **Storage**: a string IS a `HiLowArray<u8>` of UTF-8 bytes with a string type tag. Inherits array allocation, refcounted ownership, scope-death cleanup, and (later) watcher machinery. NO separate HeapType::String, NO parallel allocation path. (Package A.)
- **Mutable**, array-style (in-place ops grow/modify the buffer).
- **Indexing**: `s[i]` -> u8 byte, O(1). NOT codepoint indexing.
- **Codepoints**: represented as `u32`. NO `char` primitive type (Option 2). Codepoint access via explicit helpers (.chars, codepoint_at, iteration), O(n), cost visible at call site. Conversions: from_codepoint / to_string.
- **Length**: `.length` = codepoint count (sub-phase 2). `.bytelength` = byte count.
- **API surface** (High-mode ergonomics): slice, split, trim, replace, concat, etc. Helpers on the byte substrate, not a different type.
- **Watchers**: whole-language (Low mode too). `(changed)s` fires on in-place mutation, array-style. GC-free, codegen-driven — consistent with existing watcher machinery.
- **Mode model**: byte substrate + u32 codepoints + scalar byte indexing is Low-mode-native. High mode adds helpers on the SAME representation. One representation crosses the mode boundary with no conversion.

### Sub-phase breakdown (each finished + valgrind-verified before next)
1. Substrate: string-as-tagged-array-of-u8, literals, s[i]->u8, .bytelength, equality, concat. [PARTIAL commit d9f7268: core functionality working, memory cleanup needed for valgrind-clean completion]
2. Codepoint layer: .length (codepoint count), .chars/iteration, codepoint_at, from_codepoint/to_string.
3. Manipulation API: slice, split, trim, replace, case ops, search/contains.
4. Watcher integration: (changed)s firing on in-place mutation, mirroring array path. Done last (riskiest — watcher lifetime is where bugs hide).
