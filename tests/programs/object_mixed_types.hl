high program(): i32 {
    let person = {
        name: "Alice",
        age: 30,
        active: true
    }
    print(person.name)
    print(person.age)
    print(person.active)

    person.age = 31
    person.active = false
    print(person.age)
    print(person.active)
    return 0
}