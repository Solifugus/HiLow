high program(args: [string]): i32 {
    function makeWatcher(): nothing {
        let localCounter = 0
        return watcher(localCounter) {
            print("should not compile")
        }
    }

    let w = makeWatcher()
    return 0
}