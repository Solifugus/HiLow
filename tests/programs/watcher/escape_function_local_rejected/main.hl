high program(args: [string]): i32 {
    function makeInvalidWatcher(): nothing {
        let localVar = 42
        return watcher(localVar) {
            print("this should not compile")
        }
    }

    let w = makeInvalidWatcher()
    return 0
}