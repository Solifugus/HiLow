high program(): i32 {
    let nums = [5, 15, 25]
    let w = watcher((gone=removed)nums) {
        print(888)  // Use literal due to print system limitations
    }
    let x = nums.pop()
    return 0
}