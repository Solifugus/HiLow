high program(): i32 {
    let nums = [10, 20]
    let w = watcher((item=added)nums) {
        print(999)  // Use literal due to print system limitations
    }
    nums.push(30)
    nums.push(40)
    return 0
}