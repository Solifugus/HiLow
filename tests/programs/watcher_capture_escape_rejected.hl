high program(): i32 {
    function attach(arr: [i32]): watcher {
        let z = 42
        let w = watcher((added)arr) { print(z) }
        return w
    }
    let xs = [1]
    let w = attach(xs)
    xs.push(2)
    print("after")
    return 0
}
