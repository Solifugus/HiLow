high program(): i32 {
  let base = {
    name: "Base"
  }
  let derived = {
    proto: base,
    name: "Derived"
  }
  print(derived.name)
  // Note: Method 'this' binding is partial in Phase 7c-i
  return 0
}