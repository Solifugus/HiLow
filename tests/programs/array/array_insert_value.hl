high program(): i32 {
    let nums = [10, 30]
    let w = watcher((item=added)nums) { print(item) }
    nums.insert(1, 20)
    print(nums.length)
    print(nums[0])
    print(nums[1])
    print(nums[2])
    return 0
}