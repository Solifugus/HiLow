high program(): i32 {
    let x = 0
    function f(flag: i32): i32 {
        watcher onX((changed)x) { print("fired") }
        if (flag ?= 1) {
            return 1
        }
        x = 5
        return 0
    }
    f(1)
    x = 7
    f(0)
    x = 9
    print("done")
    return 0
}
