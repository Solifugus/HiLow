high program(): i32 {
    let t = 0
    let xs = [1]
    watcher onXs((assigned)xs) {
        print("assigned")
        if (xs[0] < 99) {
            t = 1
        }
    }
    let w2 = watcher((changed)t) {
        xs = [99]
    }
    xs = [3]
    print("done")
    return 0
}
