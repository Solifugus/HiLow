high program(): i32 {
  let parsed_time = time.parse("2024-01-15T12:00:00")
  if (parsed_time is unknown) {
    print("parse failed")
    return 1
  }
  let later = parsed_time + 2h
  let earlier = parsed_time - 30m
  print(later)
  print(earlier)
  return 0
}