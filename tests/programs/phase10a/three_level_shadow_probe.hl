high program(): i32 {
    function inner(): i32 {
        function deep(): i32 {
            let x = 0
            let y = 7
            let w = watcher((changed)x) { print(y) }
            x = 1
            return 0
        }
        deep()
        return 0
    }

    let x = 0
    let y = 42
    let w = watcher((changed)x) { print(y) }
    x = 1
    inner()
    return 0
}