high program(): i32 {
    function inner(arr: [i32]): i32 {
        let secret = 99
        let w = watcher((changed)arr) { print(secret) }
        return 0
    }
    let xs = []: [i32]
    inner(xs)
    xs.push(1)        // dead watcher should not fire
    xs.push(2)        // dead watcher should not fire
    xs.push(3)        // dead watcher should not fire
    print("after")
    return 0
}