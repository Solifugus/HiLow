high program(): i32 {
    let inner = [1]
    let outer = [inner]
    let w = watcher((deep)outer) {
        print("fired")
    }
    stealth {
        inner.push(2)
    }
    inner.push(3)
    print("after")
    return 0
}
