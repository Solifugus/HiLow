high program(): i32 {
    function attach(xs: [i32], ys: [i32]): i32 {
        let z = 9
        let w = watcher((added)xs, (added)ys) { print(z) }
        return 0
    }
    let xs = []: [i32]
    let ys = []: [i32]
    attach(xs, ys)
    xs.push(1)
    print("after")
    return 0
}
