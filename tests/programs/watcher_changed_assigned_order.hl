high program(): i32 {
    let x: i32 = 5
    watcher onChg((changed)x) {
        print("changed fired")
    }
    watcher onAsg((assigned)x) {
        print("assigned fired")
    }
    x = 5
    print("after same")
    x = 6
    print("after diff")
    return 0
}
