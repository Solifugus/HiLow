function main(): i32 {
    let sum: i32 = 0;
    let i: i32 = 0;

    while (i < 20) {
        i += 1;

        if (i > 10) {
            break;
        }

        if (i % 2 ?= 0) {
            continue;
        }

        sum += i;
    }

    return sum;
}
